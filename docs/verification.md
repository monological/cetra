# Verification: gates, goldens and deterministic renders

How this project decides that a change did what it claims, and what each instrument can and
cannot see. `AGENTS.md` owns the architecture; this file owns the method.

Three instruments, answering different questions:

| instrument | asserts | blind to |
|---|---|---|
| `scripts/gates.py` | analytic properties — a ratio, a count, a penumbra width, whether two runs agree | anything nobody wrote an arm for; a bar too slack to fail |
| `scripts/goldens.py` | 29 committed PNGs, pixel for pixel | whether the image is RIGHT — only whether it changed |
| a hand A/B | everything else | nothing, and that is the problem: it has no bar unless you measure one |

**The one rule that governs all three: a pixel count quoted without its noise floor is not a
measurement.** Every verdict below was taken at one resolution, one frame count and one flag set,
and all three move it.

**Both suites run on all three platforms** (`./build.sh --target linux|windows`, see
`docs/build-vms.md`), and most gate arms pass on Linux and Windows — they assert an analytic
property rather than a stored image, so they are renderer-independent by construction. Two things
do not travel and should not be read as regressions: **timing arms** are meaningless under a
software rasterizer, and **goldens** compare real pixels, so a genuine rasterizer difference shows
up as a difference. The corpus must not be re-baked off macOS.

## Contents

- [Running the gates](#running-the-gates) — which binaries a number describes, what a release run
  moves, why frame counts are a correctness question, how to read a `--profiler` table
- [Deterministic renders](#deterministic-renders) — the bar, the noise floor, reading a diff
- [What breaks determinism](#what-breaks-determinism) — the per-source table, and the trap that
  two runs of one build are not two builds
- [Assets, and which are safe](#assets-and-which-are-safe) — the per-asset ledger
- [The recipe](#the-recipe) — the cross-build baseline, and the six times it has moved
- [Capturing a before](#capturing-a-before)

---

## Running the gates

### Which binaries a number describes

`scripts/gates.py` and `scripts/goldens.py` both take `--bin-dir` (or `CETRA_BIN_DIR`), default
`out/bin`. gates prints the resolved directory on every run, because which binaries a number
describes is part of the result.

**`--bin-dir out/release/bin` is the fast path, and it is fast for exactly one reason** (spec
11.65, measured): `forest` starts in **6.58 s debug against 1.36 s release**, a 4.8x saving on
~55 launches, because its startup is CPU work `-O2` reaches — procedural 1K texture bakes, 20
cluster-DAG builds, the scatter, erosion. `render` goes 0.41 s to 0.36 s over ~284 launches,
because ITS startup is a GL context plus ~37 driver-side shader compiles that no optimiser
touches. So the release win is forest's, almost entirely. Requires `./build.sh --release` first,
and it is deliberately not auto-selected: a release tree older than the source would let the
suite pass on a binary nobody just built.

### What a release run moves

**Measured end to end: 14:51 debug, 10:17 release** for the same 265 arms — 241 identical, 13
moved, 0 verdict flips. Ten of the movers read milliseconds or forest pixel counts. The other
three matter: `forest-lod`, `cluster-bands` and `cluster-parity` move because **meshoptimizer
makes marginally different collapse decisions under `-O2`, so a LOD chain and a cluster DAG are
build-type-dependent**. The control is exact — `--no-lod`'s raw 65,098,553 triangles is identical
in both, so the simplifier's arithmetic moved and the geometry did not. **Forest triangle counts
are therefore not comparable across build types**, which is why gates prints the directory it used.

The cook (spec 11.99) narrows that where the key allows sharing: two builds reading one cook dir
draw whichever build cooked first, so the counts agree by serving one artefact rather than by
agreeing arithmetic. It does NOT collapse the forest movers in practice — the tree prototypes'
vertex bytes themselves differ across builds (Phase 0 measured 12 of 20 input digests moving), so
their DAG entries split by content and each build still cooks its own. The suite is unaffected
either way: gates runs against a per-invocation cache dir, so everything an arm reads was cooked
by the binary under test.

### Goldens must not follow the gates onto release

**Goldens are a different matter and must not follow blindly.** They are committed PNGs, `-O2`
changes CPU float results, and checking release-built frames against debug-baked goldens is a
comparison across two builds — which this file's own rule says does not hold. Move them only
together with `--rebake`.

### Frame counts are a correctness question, not a performance one

**And frames are nearly free, which is the counter-intuitive part.** A frame at the suite's
shared 400x300 costs ~0.0013 s: thirty frames cost **0.04 s** more than one. The suite's cost is
~339 process STARTUPS, not pixels, so lowering a frame count saves ~5 s across the whole suite
while risking an arm that reads a pose silently reading a different one. Frame counts are a
correctness question here, not a performance one — `render()`'s docstring records what the engine
actually forces, which is about two frames for a static pinned capture, and 60 / 150 / 240 / 400
where the accumulators, the meter, the trace cadence and the walk geometry genuinely demand them.

### Reading a `--profiler` table

Three things about the numbers, all of which have been got wrong at least once.

**Every row is per FRAME, not per call or per occurrence** (spec 11.97). A pass entered once in the
window and a pass entered every frame both publish what they cost the AVERAGE frame, which is what
makes the rows addable and `TIMED` meaningful. Before 11.97 each column divided by its own count and
a gated scope published a per-occurrence cost in a column of per-frame means — `dir_shadow_fixture
--gi-volume` printed a CPU `TIMED` 21x its `FRAME`, with nothing wrong in the clock.

**`FRAME (wall)` and `PERIOD` are different quantities and neither substitutes for the other.**
FRAME is the frame's own bracket, begin to end; PERIOD is begin to begin, so it also carries the
swap, the vsync wait and the poll. For the CPU column `TIMED <= FRAME` is an IDENTITY — flat scopes,
all closing inside the bracket — so a violation is a bug in `profiler.c` and never a slow frame. For
the GPU column it is a loose bound both ways, because the driver runs behind and a pass's time can
land outside the bracket that submitted it. PERIOD is the one that matches an fps counter and the
one a budget is read against; headless has vsync off, so the two nearly agree there and diverge in a
window.

**A 0.000 GPU row is not a free pass.** It is either a free pass, a driver that accepts timer
queries without measuring (which is what `gpu-nonzero` watches for), or — before 11.97 — a result
that was never collected, since an unready query was dropped rather than waited for. Five forest
rows read exactly 0.000 that way while running every frame. The ring is drained at each latch now
and a give-up is reported by name, but **any 0.000 GPU row quoted from a spec before 11.97 should be
read as unmeasured rather than as free.**

## Deterministic renders

### The bar, and the noise floor

The bar for "this refactor changed nothing" is **0 differing pixels**, and you can
only claim that from a scene that renders identically across two runs of the SAME
build. Always measure the noise floor first:

```bash
# same build, twice -- must print "0 (0)" before the comparison means anything
magick compare -metric AE run_a.ppm run_b.ppm null:
```

**Measure the floor even when this file says the asset is safe.** Every verdict in the table below
was taken at one resolution, frame count and flag set, and all three move it -- `apps/forest` reads
7,134 px at 20 frames and 34,991 at 60. A pixel count quoted without its floor is not a measurement.
The floor run costs one extra render; skipping it has already produced one confidently-stated number
that had no basis (spec 11.30's ordering comparison, caught by "is forest even deterministic?").

### Reading a diff

`-metric AE` counts differing pixels (the useful bar). `-metric RMSE` gives
magnitude, `-metric PAE` the worst single-channel delta -- `PAE` of `1/255`
(0.00392) is last-bit rounding, i.e. the compiler scheduled the same math
differently, not a behaviour change. To see WHERE two renders differ:

```bash
magick compare -metric AE -compose src before.ppm after.ppm diff.png
# sparse specks are usually LSB noise; dilate to make them visible:
magick diff.png -negate -morphology Dilate disk:4 -negate -resize 1000x big.png
```

### What breaks determinism

**"Two runs of one build match" does NOT mean "two builds match."** This is the
trap, and it is worth internalising before trusting any comparison.
Auto-exposure adapts per FRAME (a fixed 0.04 blend plus a snap deadband, not a
delta-scaled rate -- see exposure.c), so two equal-length headless runs of one
build adapt identically and compare at exactly 0 -- which looks like proof and
is not. Across two builds the approach path can differ (async texture arrival
order perturbs the frames the meter reads), exposure lands somewhere slightly
different, and since it multiplies the whole frame, **every pixel in the image
changes**.

Measured on a change that was provably a no-op (a uniform hoisted out of a
vertex shader, identity matrix, mathematically identical): **8,275,230 of
8,294,400 pixels differed (99.77%)** with auto-exposure on. The same comparison
with `--no-auto-exposure -E 1.0`: **9 pixels, PAE 1/255.** The 99.77% was
entirely exposure.

So: always pin exposure for a cross-build comparison. A whole-image diff with no
recognisable structure -- backdrop and subject shifted alike -- is the signature
of an exposure difference, not a shading bug. Render the diff map (`-compose
src`) before diagnosing; if everything is lit up uniformly, suspect exposure
first.

| source | deterministic across BUILDS? | how to pin |
|---|---|---|
| **Auto-exposure** | **Converged: YES.** The snap makes steady state a pure function of the scene, then bit-identical across runs. BEFORE convergence, no -- the approach path shifts with async-load arrival order, and exposure scales every pixel. WHEN it converges is scene-dependent: frame 91 on `postfx_convergence_fixture`, not the deadband's first touch around 18 | **`--no-auto-exposure -E 1.0` (still the rule for before/after)** -- cheaper than reasoning about whether your frame count cleared it. `--exposure-probe` prints `target_log2` and `adapted_log2`; equal means converged |
| **Wind** (`wind_response` materials) | YES in headless (spec 11.2) -- rides the fixed-step frame clock, frame N is phase N | (automatic) |
| **Spring bones** (`springbone.c`) | YES in headless -- stepped by the animation clock, which is the frame clock | `--no-springs` only to isolate spring motion in an A/B |
| **Particles** (falling leaves, spores) | YES in headless (spec 11.2) -- ticked from the frame clock; game-loop step count is exactly one per frame | (automatic) |
| **Skeletal animation** | YES in headless -- the frame clock steps a fixed 1/60 per frame so frame N is always pose N | (automatic) |
| **TAA jitter** | YES -- disabled in headless unless `--headless-jitter` | (automatic) |
| **Orbit camera** | YES -- auto-rotation disabled in headless | `--cam-eye`/`--cam-target` for exact repro |
| GTAO / SSR temporal accumulation | frame-count driven, not wall-clock; no drift observed across builds | `--no-ssr --no-ssao` if isolating |
| **A config snapshot** (spec 11.71) | **It pins the LOOK, not the frame index.** Everything above still applies underneath it: `--config` restores ~230 settings including the adaptation state, so exposure continues rather than re-approaching, but it does not restore the frame counter, the temporal histories or the async-load order. A snapshot plus the same `-f` is reproducible; a snapshot at a different frame count is a different frame | it IS the pin -- and dump `--config-dump` from the run you want to reproduce, rather than transcribing flags |
| **Cooked artefacts** (spec 11.99) | **Deterministic by construction, and the one SANCTIONED crossing of this table's rule**: the cook key carries content and recipe/library versions, never the build type, so an artefact cooked by one build may serve another. Where a producer's bytes differ across builds (the tree meshes under -O2) the content fold splits the entries with no policy; where they agree (the rocks) sharing is what happens. Bounded by the container's version-and-checksum refusal | `--no-cook` (or a fresh `--cook-dir`) whenever the measurement is OF a bake; `CETRA_COOK_DIR` to pin where a comparison reads from. gates.py and goldens.py isolate onto their workdirs automatically |

### Assets, and which are safe

| asset | wind | springs | verdict |
|---|---|---|---|
| `my_models/raiden/source/raiden_textured_rigged.glb` | no | **yes** | **best baseline.** Rigged + animated + normal-mapped; 0-identical headless with or without springs (spec 11.2 measured both). Exercises the skinned tangent path nothing else does. |
| `assets/parallax_fixture.gltf` | no | no | deterministic, but 4 vertices and no mirrored UVs -- only proves the `w = +1` path |
| `assets/abandoned_window/abandoned_window.glb` (+ sibling `.cscn`) | **yes** (curtains) | no | **usable since spec 11.2**: x3 pairwise 0 px headless with pinned exposure. The previously recorded "bistability" (269009 px between runs) did not reproduce once wind rode the frame clock -- it was wall-clock wind phase. `-m foo.glb` auto-loads the sibling `foo.cscn`; `--no-scene-file` escapes it. |
| `assets/room.fbx`, `assets/c64.fbx` | no | no | room's textures point at a dead path; c64 renders fully (textured, lit) since specs 11.1 |
| `apps/tree` | **yes** + particles | no | **NOT pixel-deterministic on the orbit path** -- measured 8,925 px run-to-run at 40 frames / 2800x1800, and 31,034 at the framing recorded above, so **measure the floor at YOUR framing before comparing anything**: it moves by a factor of three with resolution and frame count. The cause is `mouse_drag_update(drag_controller, glfwGetTime())` at **`tree.c:749`** -- and that call sits in an `else if` behind `if (player)`, so **`--player` never reaches it**, never creates the drag controller (`tree.c:1200`), and takes `engine->render_delta` instead. **`--player --headless` IS 0 px** -- measured x2 at 30 frames / 400x300 (spec 11.86), which makes it tree's first pixel-comparable framing and the one to use for any A/B in this app. It is what 11.86's normal-compression numbers were taken at. Wind, grass sway and falling leaves ARE frame-index-pure (spec 11.2); the ORBIT camera is what is not. **Spec 11.32 put a sea around it, ON by default**, and 11.35 put a seabed under the sea -- both under one `--no-water` guard, because a bed with no sea over it is a plate around a dome. **Its water is SPECTRAL by default since 11.35, not Gerstner** (`tree.c:1589`; `--gerstner-waves` switches back, and the four wave params it authors are DEAD unless you pass it). So the 45-pass/24-texture FFT cost and the FFT-only crest foam and caustics all apply to this app by default, and every cascade change in 11.33 and 11.35 reaches it. No tree capture from before 11.35 compares; none from before 11.36 does either, which re-scaled the water's absorption. |
| `apps/spores` | particles | no | deterministic headless since spec 11.2 (x3 0 px; game-loop step count is exact) |
| `apps/forest` | no (deliberately) | no | **NOT pixel-deterministic. 34,991 px run-to-run** at 60 frames / 3200x1800, 7,134 at 20 / 1600x900 -- it accumulates, and **140,910 px (9.8%) at 20 frames / 800x450 with `--taa`** (measured on the pre-11.34 build, whose `--taa` target was accidentally 2-sample; re-measure before citing against the genuine 1-sample path), so the floor is far worse on the AA path it ships than on the MSAA one these older figures were taken at. **The Hillaire sky is the source**: `--no-sky` drops it to 4 px and `--render-mode 6` to 0. Physics is exact (two `--trace-player` traces diff byte-identical), as are geometry, scatter, LOD and textures. For a 0 px claim use `--no-sky` or `--render-mode 6`, or assert on the submission integers instead. Spec 11.29 asserted determinism as a precondition and never checked it -- every `forest-*` arm reads integers, so the suite has never compared two forest frames. **Its GPU timings are a different matter and are tight**: 0.23% run-to-run at 1600x900, and **0.8% at 3200x1800 when the runs are BACK-TO-BACK** (five samples, 330.3-332.9 ms). An earlier 11% recorded here was a protocol artefact -- those two samples had four other heavy configurations between them, so it measured drift across a sequence, not per-run jitter. **Interleave configurations (A/B/A/B) instead of running A then B**, and take each config's floor from samples adjacent in time to the ones compared; done that way the native-resolution comparison resolves a 1.5% effect cleanly. **No forest capture from before 11.62 compares, even on the 0 px paths.** `windObjectPhase` now rounds an object's position to a whole unit before hashing it -- which it does whether or not the origin has ever moved -- so every tree draws a different sway phase than it did. The LOOK is unchanged, since the phases are arbitrary either way, and nothing automated reports it: the only wind fixture runs `phase_variation = 0`, which returns before the snap. The round exists to absorb an ulp of reconstruction error that would otherwise re-phase objects on every shift, and it cannot wait for the first shift without producing exactly the pop it prevents. **And no capture from before 11.63 compares at all**: forest is an ISLAND on a terrain quadtree with resident regions now, which is a different world with a different scatter (prop counts fell about 7%, which is the margin — a region draws inside its own square and clips against the domain's, where the old scatter drew inside the clipped box so every draw landed). `--no-island --no-quadtree --no-regions` is as close to the old configuration as the flags reach, and it is not identical: the quadtree draws a different surface at a different density, which is the point of it. |
| `assets/mask_fixture.cscn` | no | no | **the only visible ALPHA_MASK geometry in the corpus** (spec 11.31). An opaque quad, a MASK quad above the cutoff and one below it, in one frame. **It IS a golden**, and this said otherwise for several specs: it is the only golden rendering visible masked geometry at the 4x MSAA default, which is where alpha-to-coverage is live. Two of its four arms pass `--taa` to drop to one sample, because a fractional alpha there is *supposed* to become fractional sample coverage and the kept-quad equality only holds at one sample. What does NOT vary with the sample count is which quad is there at all -- `mask-samples` (spec 11.87) is the arm asking that, and it renders bare `--msaa 1`, the one configuration nothing else in the file exercises. |
| `assets/water_fixture.cscn` | no | no | **the water instrument** (specs 11.32 to 11.35). A ramp DESCENDING toward the camera into the water over a flat bed, so one frame carries a shoreline, a continuous absorption ramp (path grows with distance as the sight line goes grazing), and a Fresnel reflection. **0 px x2 same build** -- the surface is a pure function of the frame clock. Read it as RATIOS: R/B falls 0.2168 -> 0.1461 -> 0.0963 with distance, dry land reads 1.98. Since 11.35 the frame also carries the HORIZON, which is what `water-horizon` reads as reach invariance and `water-farfield` reads as filtering; do NOT extend the absorption monotonicity read up into that band, where sky reflection legitimately raises R/B again. Since 11.33 the fixture's own `water{}` block creates the surface and authors every key -- 21 flat since 11.84 split `scatter` into `scatterAlbedo` and `scatterGlow`, and 8 in each of the two wave trains since 11.48 -- so an arm names only what it varies, and `--no-water` is what turns it off. Note an arm overriding one field of a train has to go through `_water_cscn_variant`'s one-level merge: a plain `dict.update` replaces the whole nested object and the seven fields it drops fall back to `create_water`'s defaults, which are this fixture's own values and so would hide the mistake. Its 31 arms also drive `--water-bed dome` (shoaling), `--water-probe` (the CPU query), `--water-fft-probe` (the spectrum's variance and the transform against its closed form) and `--no-water-lod` (far-field filtering) off this one file. **Three arms author their own framing on top of it**, because the fixture's own cannot see what they measure: `water-waterline` needs crests closing OVER the eye (amplitude two orders above 0.06), `water-glitter` needs the sun dead ahead and lower, and `water-foam-persist` needs a 20 m/s sea over 90 frames AND raises the level to 1.1 to drown the ramp, so its box needs no notch cut round dry geometry. **Its sun was a lie until 11.43**: the block authored `sun_elevation` / `sun_azimuth` FLAT where `parse_env` reads them under a nested `sun` object, so the stated 26 degrees was never in effect and every arm here is calibrated against the sky's own 35. Rendering the stated 26 moves 85% of the frame. `parse_env` and `parse_water` now report a key nothing read. **The ramp's orientation flipped in 11.43** (it was a paper blade seen from beneath), which moved the waterline from row 0.5079 to 0.4530 -- so every box that reads the shoreline is now DERIVED from the wedge's corners in the glTF via `_water_ramp_band`, stated as heights on the ramp rather than as screen rows. No capture from before 11.43 compares. |
| `assets/beach_fixture.cscn` | no | no | **the surf instrument** (spec 11.44). A dome rising out of the sea, so one frame carries open water at the rim, the whole shoaling ramp, and a dry crown 0.6 units up — and unlike the ramp above it has a shoreline facing EVERY direction, which is what lets `beach-shoreline` ask whether the surf is a ring rather than a band that happens to cross the frame. **The analytic bed is a flag, not a scene key**: without `--water-bed dome` nothing shoals and the frame has no surf, no shore foam and no run-up at all, so every arm installs it. The mesh duplicates `render_dome_bed_height` deliberately — the water shoals against the analytic field and this mesh is what the eye watches it shoal against — and the gate reads the waterline (r 5.66) out of the glTF rather than re-deriving the formula, so the two copies are compared instead of becoming three. **Its arms run Gerstner**, for two reasons: crest foam is FFT-only so whatever whitewater is left is the SHORE band, and the spectral path's foam HISTORY accumulates per cascade texel and therefore tiles the world, which makes the bed measurably change the frame out to the horizon. Read as R/B against depth: 1.0123 → 0.7962 → 0.5000 → 0.3172 → 0.2482 over depths 0.29 to 5.12 m (re-measured after 11.84 re-derived its in-scatter; the smallest step is 1.27x against a 1.15 bar, the tightest of the suite's RATIO arms --
`water-shore-foam`'s exact-zero leg has less slack still), flat at 1.204 with `--no-water`. The sun's glitter path owns the right of frame, so every brightness read is taken on the left. **Its `.cscn` says `"waves": "fft"` and NO arm renders it that way**, which is a trap worth knowing before reading a hand render: opening the fixture plainly (`render -m assets/beach_fixture.gltf --water-bed dome`) auto-loads the block, gets the spectral sea, and paints a blown-out white ring over the whole shoal that no gate covers — the arms all pass `--water-waves gerstner`. 11.48 confirmed the arms really are insulated (`beach-shoal` identical to 4 dp across it, `beach-shoreline` failing at the same count), so its sea-state block is authored and unexercised. **`beach-shoreline` was a KNOWN RED for four specs — 16 of 18 azimuths at 11.44, 9 at 11.45, 7 by 11.48 and 6 after its P6 — and the ring was intact the whole time.** The arm took the brightest sample along each outward ray, and the ray crosses the DRY CROWN: sunlit sand reads 580-591 luma against a shore band at 570-607, so the argmax was decided by ties of a few codes between two unrelated surfaces, and at two azimuths the two peaks were EQUAL and the crown won on walk order. It now takes the argmax of |with bed − without bed| along the same ray, which cannot read dry sand at all because the bed does not touch it — the standing assertion of `beach-surf-zone`, whose crown samples read exactly 0 moved. That reads 16 of 18 against the SAME unmoved bar of 13. **Do not read a brightness peak on this fixture as the shore band**: the crown is as bright, which is why the note above says every brightness read is taken on the left of frame — the glitter is only half of what a ray here competes against. **The group has FOUR arms, not the three named above**: `shore-twin` rides this fixture too, reading the swash film's CPU twin against its own ceiling. |
| `assets/fog_volume_mix_fixture.cscn` | no | no | **the σ-weighted-fold instrument** (spec 11.40). Same geometry, camera and crop boxes as the fixture below; only the volume set differs. Left strip warm alone at D, right strip white D/2 **coincident with** warm D/2 — total extinction D on both, so transmittance and the depth mix are identical and the only variable is what the tint sum is made of. R agrees to four decimals, which is the control. Exists because **nothing else can distinguish the σ-weighted fold from a plain `S *= tint`**: with no global fog `airSigma` is 0 and the fold degenerates to exactly that. Adding global fog does NOT work as an instrument — 3.8 units of clear air sit in front of every box, so density drowns the box before it dilutes its tint (0.8917 the wrong way at 0.5, a uniform frame at 2.0). Measured 1.0454, and seen to fail at 1.0004. |
| `assets/fog_volume_fixture.cscn` | no | no | **the local-fog-volume instrument** (spec 11.39). One emissive backdrop and two boxes of denser air at EQUAL density differing only in tint, with a gap of bare backdrop between them as the control, so every read is in-frame. Authors no global fog at all, which is what makes `fogvol-off` also a test that the volume ARMED the pass. **Needs 60 frames, not 30**: at 30 two runs of one build differ 3/255 across the whole frame while the froxel accumulator settles; at 60 and 120 it is byte-identical and the gap reads the exact no-medium value. The classic `--fog` arming is 0 px at 30 on this same scene, so the instability belongs to arming by a per-frame published count and is **not explained**. A white tint is an exact identity (spread 0.0000) and `--no-fog-volumes` returns box/gap to exactly 1.0000. |
| `assets/absorption_fixture.cscn` | no | no | **the volume-absorption instrument** (spec 11.32). Three transmissive panels over ONE uniform emissive backdrop, same attenuation, thicknesses 0.0/0.3/0.6. The backdrop is uniform because thickness also drives the refraction bend, so a patterned one would confound absorption with which backdrop each panel covers. Zero thickness is an exact identity (G/R 1.0000) and red is authored unabsorbed as the in-frame reference. |
| `assets/sss_msaa_fixture.cscn` | no | no | **the SSS profile-tag instrument** (spec 11.37 phase 3). `.cscn`-only on `sss_fixture.gltf`: two profiles with opposite channel weights at equal radius, spheres small enough that the silhouette is a real fraction of each. `sss_skin_a` is tag 1 and `sss_skin_b` tag 2 (slots follow the order in the materials block, and the slot is printed at startup) -- only the **tag-2** sphere can show the defect, because its silhouette averages 2 with the uncovered 0 and lands on 1; the tag-1 sphere averages toward 0, is dropped, and is therefore the control. **Read by varying the profile COUNT at a fixed `--msaa 4`, never 4x against 1x** -- antialiasing dominates that comparison and moves the rim the opposite way, so it reads as the defect being absent. Measured +3.58% on the outer band with the interior at +0.19%. |
| `assets/wind_uv_fixture.cscn` | wind (deliberately) | no | **the UV1-as-wind-data instrument** (spec 11.51). Three identical quads differing ONLY in TEXCOORD_1.y (1.0 / 0.5 / 0.0), which `wind_mode` 2 reads as a flex weight. **0 px x2 same build** -- wind rides the fixed-step frame clock. Ships with a `_still.cscn` twin at strength 0, because wind has no CLI flag and a single frame cannot substitute: a linear ramp of displacement preserves the quads' even spacing, so three quads that all moved by the same wrong amount look exactly like three that moved right. Read as travel in px: 48.57 / 31.80 / 15.04, steps 16.76 vs 16.77. **The failure it guards is SILENT** -- geometry arriving without UV1 does not error, it gets phase 0 and flex 0 and still LEANS (the height-mask body term carries no flex), so a broken frame reads as a calm day. Deliberately written in Python, not exported from Blender: what cetra owns is assimp's two V-flips CANCELLING, which an assimp bump or a change to `uv_flip_flag()` would invert silently. Blender's own flip (`v_gltf = 1 - v_blender`, so author `UV1.y = 1 - flex` and put the stem at `v = 1`) is on the far side of the file boundary and is recorded in the spec, not asserted here. |
| `assets/wind_cull_fixture.cscn` | wind (deliberately) | no | **the wind-cull instrument** (spec 11.53), and it fails in two directions on purpose. Three wind quads BEHIND the camera that must be culled (exactly 3 of 6, 3 draws), and TWO whose import AABBs are entirely outside the left frustum plane and which the wind blows back into view — one CLOTH, one VEGETATION — read as **0 px against `--no-frustum-cull`**. **The vegetation quad is the whole reason the fixture tests the mechanism**: authored at `turbulence: 0` with no `windMode`, as it first shipped, `wind_max_offset` collapses to `strength × response` and none of `wind_bounds.glsl`'s six coefficients, neither `wind_flex_max` nor `wind_leaf_max`, nor any of the vegetation branch reaches the value under test — which is the branch `apps/forest` runs on. Its UV1 is authored FLAT, not as a ramp: UV0.y runs 1 at the base and 0 at the top, so a flex ramping with height meets `uv0.y = 0` wherever it is non-zero and the leaf term's `max |uv1.y · uv0.y|` comes out 0, inert. A culling arm is unusually easy to write green -- "nothing visible was dropped" is satisfied perfectly by a build that culls nothing, which is the bug this fixture exists to catch -- so neither arm is safe alone. The geometry is DERIVED: the generator computes the frustum half-width (4.4183) from the same fov and aspect the gate renders at and states the quad's offset as a fraction of it, with four `assert`s that fail loudly if a camera or wind change stops the fixture testing anything -- one of them fired during 11.53's review and caught the vegetation quad's flex-weighted terms cancelling its lean. **Falsified by hand**: zeroing the cloth margin takes culled 3 -> 4 and loses 3,114 px; zeroing the VEGETATION branch does the same and loses 4,846 px. Its light CASTS, which is what gives `cull-cascade` a row to read -- and that arm asserts the cascade culls **ZERO**, in the `shadowcull-draws` idiom: every mesh here is inside the cascade fit, so the predicate that rejects three for the camera must reject none for the light, and a cull wired to the wrong frustum passes `cull-wind` and fails this. |
| `assets/shadow_lag_fixture.cscn` (+ `shadow_lag_still.cscn`) | skeletal clip (deliberately) | yes | **the shadow-lag instrument** (spec 11.96), and the corpus's first MOVING CASTER. A box holds still until t=0.25 and then slides along +X; the sun is overhead and the caster floats above the camera, so the only thing in frame that can change is its shadow and the read is a whole-frame compare with no projector, threshold or region mask. **Ships with a PARKED twin**, like `wind_uv_fixture`'s, and reads against it rather than against an earlier frame of itself: comparing consecutive frames also discriminates, but only while the onset falls exactly between the sampled pair, so a retimed clip would silently stop testing anything. Frame 15 vs the twin is **0 px** (the hold, which is the control) and frame 16 is **8,617 px**. **The motion has to be an authored clip**: a `.cscn` cannot express a moving object, and a schedule flag driven from the render app's update hook runs BEFORE the shadow pass and passes against the very build it exists to catch. Being skinned, it covers the POSE half of the defect; the node-transform half is the `transform` group's, off a CPU probe. **Falsified by hand**: restoring the pre-fix ordering reads **0 px** and slips the onset from frame 16 to 17 — **but only with an explicit `--bin-dir`**, because the suite auto-selects `out/release/bin` whenever its forest is no older, and a mutation confined to `apps/render` never re-links forest, so a debug-only rebuild falsifies against a stale release binary and comes back green. Three geometry traps it had to find, and all three render as *nothing rather than wrong*: a single quad casts no shadow, because the depth pass culls a face and a zero-thickness plane then writes no depth; the obvious winding over the ground's corners gives a −Y normal, so the plane is culled from a camera above and the frame is empty background; and a box whose six faces are emitted in one index order has three wound inward, leaving two VERTICAL quads whose shadow under a vertical sun is a hairline — which reads as a shadow-map problem and sends you to the wrong pass. The generator now asserts every caster triangle's normal points away from the box centre. |
| `assets/occlusion_fixture.cscn` (+ `_bare`, `_mat`, `_inside`, `_near`, `occlusion_scatter`) | no | no | **the occlusion instruments** (spec 11.98), and the first fixtures in the corpus where anything stands behind anything — every other scene is fully visible or frustum-culled, so a culler that hides on-screen geometry, or hides nothing, is green everywhere but here. A portal room: four wall slabs framing a window, 300 distinct heavy meshes behind it (culled EXACTLY, as an integer read off the submission table), three visible through the gap, and one marginal mesh whose projection pokes into the window by 2.3 mask texels — wide enough that conservatism must keep it, narrow enough that an inward-rounding mutation deletes it (measured: 2,613 px). Five `.cscn` variants select five configurations from one `.gltf`: authored boxes, the material row, NO occluders key at all — the empty-context early-out no flag can reach, which is why `occl-bare` exists — the camera inside the room, and the camera almost touching a slab, where the near plane clips the wall open and only front-face classification keeps the raster honest (`occl-near`; without it, 118,794 px of visible scene culled, invisible to every other arm's framing). The scatter twin is the measured loss: one shared mesh in one instanced run, pillars hiding every third column, draws UP while triangles DOWN. **Goldens are blind to this feature by construction** — correct occlusion changes no pixel, so no golden was added and the group's 0 px identity arms against `--no-occlusion-cull` are the coverage. The generator asserts every property an arm depends on, including that the app's model recenter is a provable NO-OP: the authored world-space boxes would not follow a shift the model took. |
| `assets/skinned_cull_fixture.cscn` | no | no | **the posed-bound instrument** (spec 11.53), and **the corpus's first ANIMATED skinned asset**. `skinned_instance_fixture` is skinned but has no animation at all -- its joints sit at the origin so the pose cannot drift, deliberately, because that fixture is about which PROGRAM a mesh takes -- so nothing here could previously tell a pose-aware bound from a bind-pose one. One joint swings a panel from x ~ -6.4 (against the same 4.4183 half-width) to centre frame, read as **0 px against `--no-frustum-cull`**, plus an aimed-away arm at 2 of 2 culled. **The clip HOLDS the swung pose rather than ending on it**: the render app plays looping, so a clip ending at the read frame wraps to the bind pose exactly when measured -- which is what the first draft did, and it read as the panel being culled. **Falsified by hand**: forcing the bound back to the bind pose loses 10,000 px. |
| `assets/varying_fixture.cscn` | no | no | **the partial-coverage instrument** (spec 11.38). 24 thin splayed fins with fanned normals and a base-to-tip colour ramp, a full-frame backdrop, and one quad whose UVs are authored outside [0,1]. **0 px x2 same build.** Read with `--render-mode 12` at `--msaa 4` against `--msaa 1`: the 1-sample frame is ground truth, because coverage is a single centre test there so extrapolation is impossible rather than merely small. Every element is load-bearing, and `parallax_fixture` reading exactly 0 in all three channels is why -- a big flat quad has almost no partly covered pixels (478,677 px of surface, ~423 partly covered), and a constant normal extrapolates to itself. Opaque on purpose: alpha-to-coverage is gated on `msaa_samples > 1`, so a MASK material would change with the sample count being varied. |
| `assets/lut_fixture.cscn` (+ four `.cube` files) | no | no | **the colour-grading LUT instrument** (spec 11.58). A 24-patch chart -- a grey ramp along the top, then 18 colours over the RGB cube -- and every patch is EMISSIVE over a black base. That is what makes it an instrument: a LUT is indexed by the pixel's own value, so what the fixture must control is WHERE IN THE TABLE each patch lands, and an emissive patch maps through exposure and the tonemap alone with no N.L, no shadow and no IBL in the path. **0 px x2 same build**, range 32..244 with nothing clipped, and all six greys read exactly r == g == b -- which is what gives the neutral-axis arm a signal. **The four tables carry three different SIZES (9, 17, 33) on purpose**: size is the one header field the loader must act on, and an all-33 corpus would let a hardcoded 33 pass everything. `lut_identity` is the no-op, `lut_swap` (linear, so both interpolants agree exactly) isolates the table from the interpolation and is the only thing that catches the data block being read blue-fastest, `lut_steep` is the realistic-size interpolation read, and `lut_neutral` is the coarse probe the tetrahedral claim rests on. **That probe took two silent failures to build**, both measuring exactly zero: a SEPARABLE table preserves greys under trilinear because the off-axis weights factor out, and a CHANNEL-SYMMETRIC one preserves them because the corner pairs cancel. The shipped table lifts red alone, and the generator asserts against both wrong shapes. Read via the gate's own tetrahedral interpolator rather than the generator's closed forms -- importing the generator would make ground truth a function of the thing under test, and it writes at module scope so it would rewrite the committed assets mid-gate. |
| `assets/postfx_convergence_fixture.cscn` | no | no | **the metering instrument** (spec 11.52), and the only fixture in the corpus with `"auto_exposure": true` -- every other one pins exposure, by CLI or by authoring `post.exposure` with no `auto_exposure` key, which `cscene_apply` turns into a pin. It was written for an auto-exposure convergence gate in spec 10.2 and **no script referenced it for nine specs**; the `exposure` group wires it now. Read through `--exposure-probe`, not as pixels: all 24 goldens pin exposure, so no golden can see a metering change, which is exactly why auto-exposure was the least-tested subsystem shipping on by default. **Read only CONVERGED frames** -- the meter reads a PRE-EXPOSED frame, so its own value sits inside a feedback loop until the scene settles, which on this fixture is frame 91 -- the gap touches the deadband around 18 and is pushed back out twice first, so the arms use 150 as margin over 91, not over 18. Measured on it: gain 0.490 uniform, 0.531 centre-weighted, 0.842 spot. |
| `assets/cornell_box.cscn`, `assets/cornell_leak.cscn` | no | no | **the reference pair for derived area lights, and they are deliberately NOT converted to use them** (spec 11.49). Each authors a hand-placed light beside an emissive quad that emits a token 1 nit -- the pre-11.49 idiom -- and three gate arms work by generating a DERIVED twin at run time (`cscn_copy`) and comparing: does a fitted panel light the room the same (`emissive-match`), lift the indirect term the same (`emissive-gi`), add the same to a reflection probe (`emissive-spec`). Convert the committed fixture and there is no authored side left to compare against. The twin is generated rather than committed so the two halves cannot differ in anything the mutation does not touch. **Two traps.** `cornell_box`'s authored light sits at y 1.97 and its quad at y 1.98 while the generator's comment says the light mirrors the quad -- so they never quite matched; `emissive-fit` asserts **1.98**, the mesh's value, since the fit is a fit of the MESH, and `EMISSIVE_MATCH_EPS` 0.03 is sized for that 1 cm. And `cornell_box` cannot see an area panel's shadow AT ALL -- toggling `cast_shadows` moves no band, for the authored light either -- which is why `cornell_leak` exists: one room cut in half by a partition, where an unshadowed derived panel reads 0.41 of the lit half straight through the wall and a `light_overrides` entry takes it to an exact 0. |
| `assets/cornell_rooms.cscn` | no | no | **the clustered-specular-probe instrument** (spec 11.70), and the fourth variant `gen_cornell_rooms.py` emits. Two rooms joined by a doorway over **ONE floor mesh spanning both**, and that shared mesh is the whole design of the fixture: per-DRAW probe selection -- the cheaper answer 11.70 refused -- hands one mesh a single probe and lights half of it with the wrong room's reflections, while every per-room measurement still passes. Split the floor in two and the fixture stops being able to tell the two designs apart. **The doorway is at the FRONT**, not the middle, because a doorway in the middle puts the near half of the partition directly in front of the band where the two probes overlap -- the only place a seam can be seen at all. Lit by SPOTS rather than the area panels its emissive quads suggest, `cornell_leak`'s stated reason plus a scalloped shadow blob the panels printed at the partition base; the partition carries its own thickness (`ROOMS_PARTITION_HALF`) rather than the leak room's 6 cm, which is thinner than a shadow texel here. **It authors its own `environment` block**, and that is load-bearing rather than tidy: a probe cannot be created without a precomputed IBL, so with only the gate arms passing `--sky` the file opened by hand rendered with its probes refused -- the `beach_fixture` trap, a second time. Read as the dark room's floor against a variant that moves probe B's CAPTURE POINT into room A and changes nothing else, which isolates selection from storage: **1.82x**, with the lit room as an in-frame control at **0.0000** drift. **Was known open and is CLOSED (spec 11.76):** the stair-stepped region on the right room's floor was the specular-occlusion cone resolving to fully-lit or fully-dark either side of an epsilon, at half res and then magnified -- which is why it tracked shadows without being one. Reading the reflection lobe against the sector bitmask removed it and the band beside it together. The lesson generalises past this fixture: **a screen-space artifact that appears and disappears with an unrelated toggle is worth suspecting as a discontinuity in a term that toggle happens to move**, rather than as a property of the thing toggled. |
| `assets/emissive_fixture.gltf` | no | no | **the derived-area-light instrument** (spec 11.49), read through `--emissive-light-probe` rather than as pixels, because a panel half a metre out or sqrt(2) too wide still lights a room and no frame can tell a correct fit from a plausible one. Five emitters, each answering a question nothing else in the corpus asks. `emissive_quad` carries the only emissive TEXTURE in the corpus, half black and half white, so the 1x1-top-mip readback has an answer known in advance (0.5) rather than one measured off itself. `emissive_box` is a closed cube whose faces cancel -- planarity 0. `emissive_strip` is 2.0 x 0.25 rotated 30 degrees IN ITS OWN PLANE, which is the case the covariance principal axis gets wrong and an axis-aligned bound reads as 1.8571 x 1.2165. `emissive_split` (two strips, fill 0.2857) and `emissive_ell` (fill 0.51) are both perfectly FLAT -- planarity exactly 1.0 -- and exist because planarity does not reject non-rectangles; two of them rather than one because they fail for different reasons, disjoint against concave. **Every emitter lies on z = 0, so two overlapping in xy z-fight** -- the L and the split did, and the constraint is written beside the generator. Generated by `gen_emissive_fixture.py`; the lighting comparison lives on `cornell_box` instead, since it needs a hand-authored panel to compare against. |
| `assets/overdraw_layers.cscn`, `assets/overdraw_tiles.cscn` | no | no | **the depth-complexity instruments** (spec 11.31). `layers` is N full-frame opaque quads submitted far-to-near, so the counter must read exactly N -- which is how the budget's 2x error was found. `tiles` is a 24x24 grid of distinct meshes at complexity 1.00, built so the prepass has nothing to reject and can only cost. |
| `assets/ies_fixture.cscn` (+ five `.ies` files) | no | no | **the IES photometric instrument** (spec 11.57), and **every candela in it is a closed form of its two angles** rather than a measurement. That is the whole design: an IES reader cannot be tested against a picture, because a table resampled off the wrong plane still lights a room plausibly -- and it cannot be tested against a REAL `.ies` either, because nobody knows what a real luminaire's candela at 37.5 degrees ought to be. Ground truth is PAINTED. Five files, each answering a question no other does: one horizontal plane (rotational, most real downlights), 0..180 (bilateral -- the case a modulo fold gets wrong), 0..90 (quadrant, the second mirror), 0..360 (none, where a reader assuming a mirror renders the far half backwards), and `ies_tinytail.ies`, whose 90-degree candela is 1.2e-6 rather than 0 -- the only input the tail SNAP acts on, and so the only one that can tell an enforced exact zero from an inherited one. The horizontal ramp is MONOTONE on purpose: a symmetric one reads the same whether the reader mirrors or wraps. `gen_ies_fixture.py` **imports and runs `tools/gen_ies_table.py`** rather than reimplementing it -- the `gen_hair_fixture` pattern, holding the tool, the C reader and the shader to one arithmetic. The scene authors NO intensity, which is what exercises the peak-seeding path. **Its light is a POINT**, which is load-bearing twice over: it is what makes the roll authorable-and-unauthored case real, and it is why `ies-replace` has to build its own spots -- `spotConeFactor` returns 1.0 for every non-spot, so on this scene as authored multiplying the profile by the cone and replacing the cone with it are byte-identical, and the feature's central claim went untested for a spec cycle. Three of the six arms read `--ies-probe` and three render; **both halves are needed and neither substitutes**, because the shader is a THIRD implementation of the same table and no probe reaches it. |
| `assets/contact_local_fixture.cscn` (+ `contact_local_bare.gltf`) | no | no | **the local-light contact-shadow instrument** (spec 11.56), and the corpus's only scene with NO directional at all. A cube resting on a plane with two point lights MIRRORED about a receiver strip in front of it -- same height, same distance, both at x = 0 -- so along the whole line z = 0.55 they have identical falloff and identical N.L and their contribution weights differ by exactly their authored intensities. One is occluded by the cube and one is not, which makes a contribution-weighted fold predictable in closed form and an unweighted one immobile. **Read the RAW bytes of `--cs-debug`** -- the debug view returns early from the tonemap with no display encode, so decoding sRGB reports 0.6326 as 0.3564. **Sample the LINE, not a box**: the mirror holds at z = 0.55 and only there, and 0.02 either side already costs 2.8% of the weight ratio. The cube's front face is what the occluded ray crosses and it faces the camera at full incidence -- that is the screen-space march's hard requirement, not framing, since a ray that must pass behind a surface the camera cannot see reads no occlusion for reasons that have nothing to do with the feature. Measured strip 0.6326 against an in-frame control of exactly 1.0000; 0 px x2 same build. **Its two practicals are both map-less and there is no directional**, which is deliberate -- it exists to be the scene the pre-11.56 gate refused to run. The MIXED path (a key light with local lights folded) is reached by `contact-mixed`, which adds a shadow-casting directional as a gate variant and then a MAPPED lamp beside it: 0.6326 -> 0.8192 -> 0.8957, each step one light joining the fold's DENOMINATOR and nothing else. Drop skipped lights from that denominator and the third read collapses onto the second exactly. The bare twin drops the cube from the SAME vertex arrays, so an arm swapping the model path varies the occluder and nothing else. Its generator carries seven asserts and **the binding one is the SCREEN clamp, not the march reach** -- `MAX_UV_LEN` cuts 0.8 units of world march to 0.72 here, so a fixture checked only against `csDistance` can be short by a factor of two. |
| `assets/layer_fixture.cscn` (+ 7 painted maps) | no | no | **the layered-surface instrument** (spec 11.60). A FLOOR and a WALL sharing one layered material, and the perpendicular normals are the point: the projection is world-aligned, so a fixture with one plate tests one axis of it and reads the same whether the other two exist or not. **0 px x2 same build.** Every arm reads `--render-mode 6`, where the shader returns `linearToSRGB(factor * sRGBToLinear(blend))` against a white factor -- so a correct renderer hands back the exact painted byte, and `layers-select` reads 128 / 48 / 208 off by ZERO. A lit read would be a claim about the BRDF as much as about the blend. **Layer 1 is a CHECKER, not stripes, and that took two attempts**: the wall sits at constant z, so a projection frozen on Y still varies stripes correctly along x -- the wrong answer and the right one are the same picture. It is also the only layer with a RELIEF surface map, added in review: a flat tangent normal makes `triplanarBlendNormal` an exact algebraic identity, so its three axis-sign flips could be deleted for 0 px, and per-layer roughness and AO had no coverage at all while every layer shared one map. Giving layer 1 its own moved the golden 16,468 px. **UV0 is authored DIFFERENT from UV1** (doubled and v-flipped) for the same class of reason -- authored identically, "the splat reads UV1" is untestable. The flat map is still shared by layers 0, 2 and 3, which is what exercises `material_texture_layer_for`'s dedup: three uses, one array layer, and the build logs 7 layers for 5 files. |
| `assets/layer_vt_fixture.cscn` (+ its own splat; maps shared with layer_fixture) | no | no | **the composite-cache instrument** (spec 11.66). A FLOOR and a 45-DEGREE RAMP on one layered material whose splat is authored in WORLD XZ -- which is the whole reason it exists beside layer_fixture: that one's splat is UV1-space and the cache serves world-XZ splats only, so no arm on it can see the cache in either direction. **0 px x2 same build.** The ramp is the anti-vacuity and the reason it is 45 degrees exactly: on the flat floor the cached path is byte-exact by construction (macro of top-mip means, detail ratio of map over mean), so a floor-only fixture could not tell the cache being ON from the flag doing nothing -- the ramp sits exactly between the Y and Z projections, where the two paths legitimately differ. Measured: cached vs per-texel 2,330 px of 480,000 at the derived 256 (splat-resample edges plus one patch at the blend crossover, the predicted interlock loss), byte-exact 128/48/208 at every column centre, and at a FORCED 8x8 atlas -- a texel wider than a checker period -- the columns still read exact while the checker still alternates 32/224 through the detail term, which is the frame a plain baked atlas cannot produce. Splat rows: ramp = checker alone, floor near-band = the four selection columns, floor mid-band = the layer 2-into-3 transition the invalidate arm watches move (0.697 to 0.497) across a `--layer-blend-at` re-bake. Reuses gen_layer_fixture's painted maps by name, so the codes the arms predict have exactly one source; its generator asserts the coarse texel exceeds a checker period and that columns align to the coarse texel grid. **Since 11.67 the RAMP DRAWS FIRST and the order is load-bearing**: the feedback vote pass's occlusion claim rests on its depth test, and a hidden surface drawn before its occluder is also hidden by painter's order -- with the occluder last, deleting the depth test changed nothing and the mutation round could not see the deletion. Occluder first makes depth the only rejector, and the reorder moved the lit golden on the floor/ramp coplanar seam (the draw-order-tie class), rebaked deliberately. Its five 11.66 arms were joined by six 11.67 ones: pages identity/effect/churn/walk and feedback sky/occlusion, the last discriminating feedback from prediction (12 pages voted behind the ramp against a frustum wanting 25). **11.68 adds four ROAD arms and the fixture stays road-free to carry them**: every road arm builds a runtime `cscn_copy` variant, so the golden and the sixteen arms before them render an unmutated file and read byte-identical numbers. The generator gains the road's constants and their asserts (the band fits the floor; the pages arm's shoulder sits between a coarse page texel and a coarse fallback texel) but writes the same three files byte for byte. Two traps the mutation round found, both worth knowing before writing an arm here. **A whole-frame pixel count cannot see a road**: two legs of the composite cache differ by a code or two across every flat surface in view, so `layers-road-pages` passed with the road deleted until it was rebuilt on a byte read. And **an arm that moves the camera must move the projector too** — `_projector` builds from `_cscn_camera`, which reads the COMMITTED scene, so an arm passing `--cam-eye` samples pixels the render never drew and reads plausible bytes off the wrong part of the frame; pass `_cscn_camera(name, eye=..., target=...)`. |
| `assets/decal_fixture.cscn` (+ three painted images) | no | no | **the clustered-decal instrument** (spec 11.73). A floor, a wall facing the camera, and an OBLIQUE PLATE, with a poster on the wall and a scorch on the floor. **0 px x2 same build.** **The poster is painted in four QUADRANTS and that is the fixture's whole lesson**: every earlier version of it was invariant under mirror, flip, transpose AND 180-degree rotation, so the shipped projector -- which laid every image on backwards and upside down -- painted a frame eight arms could not tell from a correct one. A single-colour patch cannot test a projector. The generator asserts the four codes separate in every channel, so each wrong transform gives a different wrong answer. **The oblique plate is the other anti-vacuity**: it sits INSIDE the poster's box and past its angle fade, so a correct build leaves it at its own substrate -- delete the facing test and the poster smears onto it. `decals-albedo` alone passes on a build with no facing test, and `decals-angle-fade` alone passes on a build that projects NOTHING; neither is safe without the other. **The plate stands in FRONT of the wall, so where it sits decides what every arm reads** -- its first draft covered the poster's own read point and the albedo arm reported a decal that landed as one that did not. Read points are generator constants, asserted clear of the plate, inside the opaque region, and ON THE SURFACE being read: a point at the box's own centre hangs a quarter unit in front of the wall, which is fine mid-quadrant and wrong at an edge. Ground truth is PAINTED and read as a byte through `--render-mode 6`, and the substrates are `pow(1/2.2)` of the authored factors -- **not** the piecewise sRGB curve, which is a two-code error on the floor. **Two guards here were measured INERT and the fixture was changed rather than the claim softened**: the half-texel UV inset moves 0 px (its images carry a transparent margin, so a wrapped blend is between two transparent texels) and has no arm, recorded in the docstring; the edge FEATHER was the same failure a second time -- the margin retired the alpha at |local| 0.75 while the ramp began at 0.88 -- and is now 0.35, with the generator asserting the two bands overlap. The scorch's normal map was a flat 128/128, which encodes 0.0039 and tilts the surface a third of a degree toward its own normal, so the whole tangent frame was unobservable; it is a 39-degree tilt now. Its `environment` block is `"mode": "sky"`, not `"sky": true` -- the first draft authored the latter, which parses, warns and renders a scene whose probes would be refused. |
| `assets/purkinje_fixture.cscn` | no | no | **the scotopic-shift instrument** (spec 11.83). COLUMNS ARE A LUMINANCE LADDER over 7 stops, ROWS ARE CHROMATICITY held constant -- which is the whole reason `lut_fixture` could not be reused. That chart is the right IDIOM (emissive over black, so a patch's value is a property of `emissiveFactor` alone) and the wrong SHAPE: its colours all sit at one magnitude, so it shows how the blue shift varies with luminance and not how the DRAIN does, which is the axis this feature turns on. **0 px x2 same build.** Six rows, each answering something no other can: neutral (grey is an EXACT fixed point of desaturation, so anything that moves it is the TINT -- a desaturate-only build reads 0 codes here, not nearly 0), red (rods see it worst, V'(610) about 1/32 of V(610)), green, blue (the row a TINT-ONLY build pushes the WRONG way, raising its saturation where it lowers red's), a mid-saturation warm for the cube's interior, and the rod tint's own chromaticity as a near fixed point that must move LEAST. **Its trick is that the `.cscn` authors `post.exposure` well above 1**: absolute radiances stay scotopic, which is what the rod ramp reads, while displayed codes land in readable 8-bit territory -- and if `oneOverPreExposure` is ever dropped the whole ladder shifts by `log2(EXPOSURE)` and `purkinje-absolute` reports it. **Two generator asserts came out of building it and both caught real errors**: the ladder must STRADDLE the shipped ramp (the first draft did not), and the chart must be a MINORITY of the frame (measured 0.140 against a 0.20 bar) -- the meter keeps the brightest 30% of the population, so a chart filling that tail makes the frame's metered mean its own bright rungs, closes the global gate, and renders its own subject unshifted. **That trap then bit `purkinje-lamp` for real**: `moon_fixture` at its shipped 49x reads a global gate of exactly 0.0000, so the arm renders a 12x variant. Read colour at rung 5 and rung 1, chosen by measurement -- 7 stops cannot all be legible in 8 bits and the dimmest rung lands at code 2 -- and with `--no-purkinje-acuity`, since acuity pools across patch boundaries and lifts a dim rung toward its brighter neighbour. |
| `assets/clearcoat_fixture.gltf` | no | no | **the clearcoat instrument** (spec 11.86), and for six specs the only one of the three KHR material-extension fixtures with no coverage at all -- `sheen_fixture` and `specular_fixture` have had goldens for specs. That gap is why 11.85 shipped a coat normal decoded from a channel BC5 does not store, z = -1, a normal pointing INTO the surface, past a fully green suite twice. **0 px x2 same build.** Three spheres: a coated dielectric, the SAME base with no coat, and a coated near-black metal. The uncoated one is the fixture's whole design -- under `--clearcoat-debug` it reads EXACTLY black, which a normalized vector cannot encode, so it catches a build that writes the geometric normal everywhere and nothing else does. Both coated materials carry a procedural carbon-fibre WEAVE as a clearcoat normal map, embedded as a data URI, and it is the corpus's only tangent normal reaching `pbr_frag` through a path of its own: assimp exposes it at `aiTextureType_CLEARCOAT` **index 2**, which `import.c`'s index-0 mapping table cannot reach, so it takes a bespoke read. **Its two debug reads are orthogonal and neither substitutes**: with the Z rebuild removed mean blue falls 237.8 -> 17.5 while the weave contrast barely moves; with the map ignored the contrast falls 5.99 -> 0.40 while mean blue reads 240. The golden AUTO-FRAMES to match its two siblings and the gate group pins its own tighter camera, which is not a contradiction -- a golden wants comparability, an arm wants the weave big enough to resolve a twill cell. Its generator's docstring carried three stale claims until 11.86, including "materials are flat factors (no textures)", untrue since the weave arrived. |
| `assets/texcomp_fixture.cscn` (+ 3 painted maps) | no | no | **the block-compression instrument** (spec 11.85), and **the only fixture in the corpus that MINIFIES**. Every golden here is 0 px with every mip level painted SOLID BLACK, and so is the raiden recipe -- they sit near the camera and never sample a level above 0, while most of a compressed texture's bytes live in the chain. This is a plane running 1 to 400 units out under a camera aimed down it, so one frame sweeps a few texels per pixel to hundreds. **0 px x2 same build.** Three maps, one per format, each shaped so its format can fail: the albedo is a HUE WHEEL over a luminance ramp because DXT quantises to RGB565 with four colours a block and its error is worst where a block spans hues at one luminance (a photo hides that; flat patches report DXT as free); the normal's x and y use different frequencies AND a phase offset so no block sees dy as a function of dx, which is what tests BC5's SEPARATE per-channel endpoints; the roughness is genuinely ONE channel, since a three-channel grey would exercise the DECLINE path while looking like it tested BC4. **Its arms are bounded both ways**: storing normals as DXT5 instead of BC5 leaves the byte ledger IDENTICAL at 1.833 MB, so `texcomp-format` reads the format NAME, and `texcomp-colour` carries a FLOOR because a dead colour switch reads RMSE 0.0000 and would pass a ceiling alone. **Its camera block cost two drafts**: the key is `eye`, not `position`, and a wrong key parses, warns nowhere and silently leaves the auto-framed camera in place -- the same trap `decal_fixture` records for `mode` and `beach_fixture` for its waves. |
| `assets/terrain_fixture.r16` | no | no | **the heightfield instrument** (spec 11.59), and the corpus's only fixture that is NOT a scene. What is under test is a height FIELD and a sampler over it, so the fixture is a 256² headerless 16-bit heightmap and the consumers are `forest --heightmap` plus the two probes -- there is no `.gltf` and no `.cscn`, because a terrain fixture cannot be either. **Ground truth is PAINTED**: every sample is a closed form of its two normalised coordinates, so the gate knows what the sampler owes at any point, including BETWEEN nodes where a filter is decided. Read at 0.47 codes on the lattice and 53 codes at a cell midpoint. **It carries a near-Nyquist band on purpose** -- 7.5 samples per cycle, which is the regime forest actually bakes in -- and that is the fix for its first version, which at 85 samples per cycle was so smooth that bilinear and bicubic differed by **five codes** and `terrain-bicubic` was green against a bilinear sampler. The generator asserts the separation now rather than assuming it, alongside asymmetry (a transpose must move >500 codes, the single most common heightmap bug and one that renders as plausible terrain) and headroom. **Probe rows snap onto NODES**, which is what keeps the mapping arm and the filter arm from contaminating each other: on the lattice every correct sampler is exact, so a transpose cannot hide behind interpolation error. |
| `assets/moon_fixture.cscn` | no | no | **the moon, framed** (spec 11.82), and the corpus's only fixture that exists to be LOOKED at rather than measured -- a near-full disc filling most of the view over the star field, which is the one thing about that feature no number reports. **0 px x2 same build.** Loads with no flags: it authors its own sky, sun, stars, moon, camera and pinned exposure. **Borrows `aerial_fixture.gltf`** (the `sss_msaa_fixture` precedent) because a `.cscn` must name a model and nothing of it is in frame -- the camera looks 40 degrees up through a 40-degree field, so the horizon sits 20 degrees below the bottom edge. Two things are deliberate and both read as arbitrary. The sun's azimuth is 20 degrees off opposition, **not** at it: dead full puts the light head-on, where Lommel-Seeliger correctly flattens the relief and only albedo survives, and 20 degrees lands the terminator just inside the right limb where the crater walls shade themselves. And the size comes from `moon.size 49` rather than from widening the shared `sun_disc_deg` -- the latter reproduces an earlier hand render exactly, including a much wider aureole, but authors a 26-degree SUN disc that is invisible only because the sun happens to sit below the horizon, which any later elevation change breaks silently. |

### The recipe

```bash
# rigged + animated + normal-mapped, byte-identical ACROSS BUILDS:
./out/bin/render -m my_models/raiden/source/raiden_textured_rigged.glb \
    -t my_models/raiden/textures -e my_models/studio_small_03_8k.hdr \
    -a my_models/animations/strut_walk.fbx -s my_models/animations/T-Pose.fbx \
    -x -f 120 --no-springs --no-auto-exposure -E 1.0 -S out.ppm
```

`--no-auto-exposure -E 1.0` is load-bearing: it stops the exposure drift
above. `--no-springs` is no longer required for determinism (springs ride the
frame clock; measured 0 px x2 without it) -- keep it in the recipe anyway so
new baselines stay comparable against ones captured before spec 11.2, and use
it on its own to isolate spring motion in an A/B.

### The six times the recipe has moved

A baseline captured before any of these will not compare against one after. Read the list before
concluding that a diff against an old capture is your change.

**This recipe's output has moved twice on the hair, ~0.12% each time**, and both
are small enough to read as noise and are not. The rig carries an alpha-blend
mesh, so it is the one real asset that goes through OIT, and the diff is the hair
silhouette and nothing else on both occasions:

- **spec 11.17**, when OIT went on by default. `--no-oit` reaches the old frame.
- **11.17's review round**, when moment generation stopped shading a frame it
  discarded. The moment reconstruction is ill-conditioned where layers sit at one
  depth -- which is what hair is -- so it amplifies the last-bit differences that
  reshuffling the shader's arithmetic produces. `--no-oit-moments` compares 0 px
  across that change; nothing else in the frame moves either way.

So a baseline captured before either point will not compare against one after.
The analytic OIT gate is the instrument that did NOT move (bit-identical across
both), which is why the hair is read as sensitivity rather than regression.

**And a fourth time, back on the hair — but this one is a behaviour change, not
sensitivity.** Spec 11.26 gave translucent casters a partial shadow under
`--translucent-shadows`: raiden's `Hair_1` (ALPHA_MASK) cast nothing and now
casts, and `Hair` / `Cool_Touch` / `rai_decal` (ALPHA_BLEND) cast solid black
and now attenuate. **331,967 px (4.0%), PAE 33/255**, and the diff map is the
groom and the shoulder under it and nothing else. It is opt-in, so the default
recipe is unaffected; `--no-translucent-shadows` reaches the pre-change frame at
0 px. Note the recipe's `-e <hdr>` is what makes this visible at all — the HDR
key lights cast, where the fallback three-point rig does not, which is why most
of the fixture corpus cannot see this feature.

**Then it moved a third time, and this one is the whole frame, not the hair.**
Spec 11.24 turned output dither on by default, so the 8-bit write now carries a
+/-1 LSB triangular dither: **5,691,710 of 8,294,400 px (68.6%) differ, at PAE
exactly 1/255**. Every pixel that moved moved by one code and none by more --
the signature this file calls last-bit rounding -- so it is a re-quantization,
not a shading change. `--no-dither` reaches the old frame at 0 px. Use it for
any comparison against a baseline captured before 11.24, and for any gate that
reads single pixels or high-frequency residuals rather than whole-frame
structure; four in `scripts/gates.py` had to be given it.

**And a fifth time, four goldens by 26 pixels total, and this one no flag
reaches.** Spec 11.28 gave `pbr_vert.glsl` an `instancing.glsl` include, so the
per-object transforms arrive through a select rather than straight off a
uniform. The driver schedules the vertex float math differently for it:
`guard_thin_panel` 1 px, `skin_curvature_fixture` 1 px, `skin_curvature_sss` 6
px, `oit_sphere_moments` 18 px, all at **PAE exactly 1/255**, diff maps showing
isolated specks with no structure. Re-baked at 11.28.

**`--no-instancing` does NOT reach it**, and that is the part worth carrying:
the flag governs whether draws are batched, where the movement is the shader
edit itself. Three of the four fixtures batch nothing at all -- `oit_sphere` is
3 meshes in 3 draws -- which is what identifies this as codegen rather than a
behaviour change. The batching's own identity is separately exact: instanced
against non-instanced on a 131-node fixture compares at 0 px, because the UBO
carries the same floats into the same arithmetic.

**And a sixth time, the largest yet, and no flag reaches this one either.**
Spec 11.31 stopped the opaque lane blending geometry that is not translucent.
An ALPHA_MASK fragment above the cutoff used to write fractional alpha and get
lerped toward the CLEAR COLOUR -- the opaque pass runs before the skybox -- so
the groom was being darkened. **1,058,960 px (12.8%), PAE 103/255**, and the
diff map is the hair and nothing else. Both directions and both correct:
brighter where a strand was lerped toward the clear colour, darker where a dark
strand now fully occludes a bright one instead of half-showing it, which is
what glTF's binary MASK means. Any baseline captured before 11.31 will not
compare.

**No golden moved with it, and that was worth knowing before trusting a green
golden run on anything masked.** `translucent_shadow` carried the corpus's only
other MASK material and it is a CASTER HELD ABOVE THE FRAME -- what the golden
measures is its shadow. So at the time no golden contained visible masked
geometry and all 21 passed either way. **`mask_fixture` is a golden now** (spec
11.87 baked one, and it moved: both masked quads had been rendering as a
screen-door stipple where glTF says a fragment at or above the cutoff is fully
opaque), so that gap is closed for the shading path. It is NOT closed for the
mip chain, and that is the next paragraph.

**No golden can see a masked material's MIP CHAIN, and 11.88 is what that cost.**
Coverage preservation only bites where a cutout MINIFIES, and the one golden
carrying a MASK material with an albedo texture is `translucent_shadow`, whose
image is a 512x4 linear alpha RAMP -- symmetric about the threshold, and a box
filter of a linear ramp is another linear ramp, so the rescale barely moves it.
`mask_fixture` declares no textures at all. So spec 11.87 shipped a chain that
painted distant cutouts SOLID with all 29 goldens at 0 px and its own gate green
-- the gate bounded the mid/near ratio only BELOW, and saturation pushes that
ratio UP. `assets/alphacov_fixture` and the `alphacov` group are the whole
instrument, bounded in both directions for that reason; spec 11.88 carries the
matrix of which of the three fixes each arm actually sees, and the answer is one
of three. **Spec 11.100 repeated the lesson one instrument down**: its deep arm's
first draft counted bright pixels, and the count read the DISTRIBUTING build
lower than the vanishing one -- grazing anisotropy averages a sparse dither into
a smooth low-alpha field the sharpened test deletes, so the arm now reads a
camera-facing quad by MEAN luma, and the grazing loss is recorded as the
technique's ceiling rather than the arm's. The old "correctly vanishes past the
uniform level" sentence is retired: distribution answers there, `alphacov-deep`
is its instrument, and the ledger row that cannot be armed (a rewritten level 0
-- the dot grid is binary there, so the fixture is blind) is proven live at the
cook-artefact byte level instead.

**`apps/tree` and `apps/forest` are the feature's only in-tree consumers and
neither is a golden**, so a change to the mip chain moves both leaf atlases at
every level with nothing automated able to see it -- the `forest-*` arms read
submission integers. Compare canopy captures by hand before believing a green
suite on anything touching `texture_alpha_coverage`. And do not reach for
`assets/ivy_arcade` as the hand-A/B content, natural as it looks: it is NOT
pixel-comparable -- measured 8,674 px between two pinned runs
(`--no-auto-exposure -E 1.0`) and 9,681 unpinned at 800x600, so anything under
~9k px on it is noise. Forest's `--render-mode 6` at a fixed camera (0 px) and
`--no-sky` (40 px) are the floors that work.

### Capturing a before

Capture the "before" from the base commit BEFORE starting work (`git stash`,
build, render, `git stash pop`). Both builds must be complete -- **never pipe
`./build.sh` through `head`**, which SIGPIPEs the build mid-link and leaves a
stale binary that renders the old code.

**And run both legs `--no-cook` when the change touches anything a bake produces** (spec 11.99).
The cache is content-keyed on INPUTS, not on the code -- a changed bake whose recipe version was
not bumped computes the same key, the after leg silently renders the before's artefact, and a
real change reads 0 px. The inverted-confidence failure this file exists to prevent, in its
newest form.

For a pure refactor there is a stronger check than pixels: diff the generated
GLSL. `out/include/cetra/shader_strings.h` is reproducible (sorted), so unescaping
both versions and diffing comment-stripped proves "no shader code changed" directly,
without going through a renderer at all.
