# A shore that dissolves, a beach that remembers, and sand that dries

## Context

11.44 shipped a surf. It arrives, it breaks, it runs up — and it still reads wrong in two
specific ways the eye catches immediately:

- **The swash is a solid line.** `water_frag.glsl:223` is the whole shore band:
  `WATER_SHORE_FOAM = 0.92`, resting at `WATER_SHORE_FOAM_REST = 0.3`. A scalar strength
  becomes a colour. Real whitewater is a *lacework* that fragments into clumps as it thins.
- **The beach forgets.** Foam accumulates per **cascade texel**, so it tiles the world and
  has one decay rate. Nothing is left on the sand behind a retreating wave.

And the sand itself never changes: `ground_beach_color` (`apps/tree/src/ground.c:147`) paints
a fixed wet band below `GROUND_WET_SAND_M` 0.18 m, baked once into vertex colour at mesh build
(`:281`, `GL_STATIC_DRAW`). `ground.h:143` already says what is missing, in its own words:
*"what is wet ABOVE it at any moment is the swash's business, not a band's."*

**What prompted this plan.** First the wet-sand ask; then `github.com/tompng/gpuocean` as a
reference, whose shore looks right. It was cloned and read in full (2,377 lines). The gap is
not the wave model — it is a solver, a foam *pattern*, and two decay rates.

**Outcome:** a surf line that erodes into clumps instead of fading as a bar; foam the beach
keeps until the next wave eats it; crest foam on the Gerstner path for the first time; sand
that darkens where the swash reached and dries behind it; and, last, a real swash film.

---

## What the reference does that we do not

| | gpuocean | cetra today |
|---|---|---|
| Swash | 256 columns x 64 Lagrangian nodes, shallow-water pressure form, 4 substeps/frame (`chain.js`) | `shoreRunup()` closed form, stateless |
| Foam shape | noise pattern eroded by a **rising threshold** — thin parts vanish first | foam value -> foam colour |
| Foam onset | one-pole rise, `FOAM_RISE = 0.08 s` | instantaneous |
| Foam decay | `foamLife` 4 s, **and 0.5 s where a wave floods over it** | one rate |
| Foam frame | world-space for waves, **(band, column) material space** for the film; only the MASKS blend | one frame, per cascade texel |
| Breaking | Jacobian **and** `H/h` depth-limited | Jacobian only -> FFT-only crest foam |
| Wet sand | none — the wet look is *real water* still on the beach | a painted band |
| Shoreline | authored analytic curve + `colT()` arclength | none; `WaterHeightFn` is opaque |

Two quotes worth carrying into the code, because both are traps:

> *"A linear spring law is NOT usable here… the pressure form pins the equilibrium to
> 'surface flat' uniquely."* — `chain.js:6`

> *"each erodes its own pattern lookup in its own frame and only the resulting MASKS blend.
> Eroding a single pattern at blended coordinates smears it into streaks."* — `ocean.wgsl:373`

---

## Ordering

Value first, architecture last. Every phase ships alone and is separately bisectable.
P1–P4 need no new data; P5 is the enabler for P6–P7.

---

## P1 — foam is an eroded pattern, not a strength

The single highest look-per-cost item, and it needs no solver.

Three mechanisms, all in `water_frag.glsl` where the foam is composited:

```glsl
mask = smoothstep(0.0, EDGE, pattern - (1.05 - 1.15 * acc));   // erode from the thin parts
acc  = mix(gen, prevAcc, foamRise);                             // ~0.1 s onset
pat  = mix(patFine, patCoarse, 1.0 - smoothstep(a, b, stretch)); // stop streaks shrinking
```

**The sampler problem, and its answer.** `water_frag` is at **16/16 declared samplers** — the
project's Wall 1 — so a foam pattern texture has nowhere to bind. `include/noise.glsl` is only
interleaved-gradient (a screen-space dither, wrong for this) and the cloud noise is a CPU-baked
texture. So the pattern is **procedural ALU**: a new `include/foam_pattern.glsl` with a hash and
a 2-octave value noise, sampled in the shore band's own coordinates. Costs arithmetic instead
of a unit, which is the trade this program has to make.

The rise term needs the previous accumulation, which `water_foam_frag.glsl` already keeps
(`foamHistoryAvailable`, `foamDecay`) — this is a second use of an existing history, not a new
target.

**Bar:** both water goldens are Gerstner with no bed and no crest foam, so they should move
**0 px**. `beach_fixture` and `water_fixture`'s shore band move and should. New arm
`beach-foam-clumps`: the foam mask's connected-component count rises as the band decays,
where a solid bar's would stay at 1.

## P2 — the breaking criterion that is not the Jacobian

```glsl
genSurf = smoothstep(0.55, 0.9, waveHeight / depth) * smoothstep(0.0, 0.5, depth);
gen     = max(genJacobian, genSurf);
```

Depth-limited breaking (`H/h`) does not care which wave model made the height, so **this gives
the Gerstner path crest foam for the first time**. CLAUDE.md currently records crest foam as
FFT-only *by construction* because the Gerstner steepness is clamped so its mapping cannot
compress — that sentence changes with this phase, and the docs go with it.

**Bar:** FFT frames barely move (the Jacobian already fires there). Gerstner frames gain foam
where the bed is shallow — which is the point. `water-crest-foam` may need its FFT-only
precondition relaxed; if an arm asserts Gerstner has no crest foam, that arm was asserting the
limitation, not the behaviour.

## P3 — foam the beach keeps

Two decay rates, selected by whether water is over the foam *right now*:

```glsl
swallowed = smoothstep(...,  edge - h);          // shoreRunup() already gives edge
decay     = mix(foamDecay, foamDecaySwallow, swallowed);
```

Slow on drying sand (seconds), fast where the next wave floods it (sub-second). The test is
nearly free because `shoreRunup().edge` is exactly "where the water is", and `shore.glsl`
already publishes it with no sampler.

`--no-water-foam-history` stays the bisect lever. **Bar:** a new arm `beach-foam-persist`
reading foam above the waterline at two frames a half-period apart — it must survive the
retreat and be erased by the next arrival.

## P4 — wet sand with a memory (the original ask)

March back ~3 primary periods in taps of `T/3`, **accumulating** rather than taking a max:

```
h = WorldPos.y - waterLevel
for k in 0..N-1:
    e     = shoreRunup(WorldPos.xz, time - k*dt).edge
    cover = 1 - smoothstep(-s, +s, h - e)
    wet   = 1 - (1 - wet)  * (1 - cover * decaySlow(k))
    film  = 1 - (1 - film) * (1 - cover * decayFast(k))
wet = max(wet, waterTable(h))
```

Accumulation is what makes history legible: the lower beach, hit by every wave, saturates dark;
the upper beach, hit by one in twenty, stays barely damp. A max gives a step. The water-table
floor stops the lower beach flickering dry between waves.

**Shading, from Lagarde rather than from guesses:**

- **Porosity darkening** `Kd *= (1 - alpha * Gp)`, alpha ~0.35–0.45 (inside Lagarde's stated
  25–50% band for natural materials).
- **`F0 -> 0.02` and flatten the normal toward `Ng`.** A water layer's air/water interface is
  *less* reflective than sand's 0.04 — the surface reads shinier because it goes **smooth**.
- **The hue shift.** Compute the Beer-Lambert number FIRST: water's `(0.0035, 0.0004, 0)` per cm
  over a 0.1–1 mm film is `exp(-0.0007) ~ 0.9993`, i.e. nothing. This session already shipped and
  reverted exactly that mistake once (the Cox-Munk capillary term, measured to zero). Use the
  mechanism that actually causes it — Lekner-Dorf internal reflection, `a_wet ~ a_dry^p`, p > 1 —
  which saturates correctly and composes with the porosity term.
- **Two-phase drying**: fast film drain (seconds), slow pore evaporation (tens of seconds).
  The shape is right per the TVBRDF work; the constants start eyeballed and are flagged as such.

**Zero new samplers.** `shoreRunup` is a pure function of (p, t) over scalar uniforms — that is
the whole reason `shore.glsl` exists (P0, landed as `3fae9ad`). `pbr_frag` needs one line,
`uniform float time;` — `render.c:348` already publishes it per program switch; the fragment
stage simply never declared it.

**Cost control.** Analytic ceiling `edge <= 1.35 * r2 * upm` is a uniform-computable scalar, so
dry sand runs no taps. Taps share train evaluations: `swPrev(t_k) = sw(t_k - T)`, which at
`dt = T/3` is `sw(t_{k+3})` — N taps cost N+3 evaluations, not 2N.

**Gating.** `Material.shore_wetness` defaulting to 0, in the shape of `wind_response`
(`material.h:119`), uploaded at `render.c:145-270`. Shore floats published by a new
`water_bind_shore()` from the program-switch block (`render.c:407-422`). `--no-water-wetness`
is the exact pre-change frame. **Nothing in the corpus opts in, so all 24 goldens stay 0 px.**

**tree wires it up** (`tree.c:1254-1263`, `:1273-1282`): island material, seabed material *and*
the shore boulders — the first two share `ground_beach_color` and would seam at the rim
otherwise. `ground_beach_color` drops `GROUND_SAND_WET` / `GROUND_WET_SAND_M` from the
above-water side and keeps the submerged pale ramp; note it has **four call sites**
(`ground.c:232, :281, :367`), not one. Re-measure whether the 128 rings (`tree.c:276`) can come
back down now the band is per-pixel. Fix two stale comments found in passing: `tree.c:270`
(0.35 m of a 2.16 m rise against defines of 0.18/1.70) and `tree.c:239` (slope 0.145 against
`GROUND_BEACH_SLOPE` 0.09).

**The fixture must be able to see it.** `beach_fixture.gltf` authors the dome emissive over a
**black** base colour (`gen_beach_fixture.py:132-142`), so an albedo change is invisible today.
Regenerate with a lit sand `baseColorFactor` alongside the emissive; author `shoreWetness`
through the `.cscn` materials block (one row in `cscene_apply.c`'s table). Arms: `beach-wet`
(swash band darker, **dry crown 0 px** — the half that matters), `beach-wet-history`
(brightness rises monotonically up three boxes; a band gives two levels, a memory gives a ramp),
`beach-wet-moves` (px between frames 30 and 44 above a floor).

## P5 — the shoreline, extracted from the bed

The enabler for P6 and P7, and the object that has been missing all along.

`_water_bake_bed` (`water.c:488`) already sweeps 256² on the CPU with the height callback. Add a
pass that **marches the zero level set** and emits a shoreline polyline: per column a world
position, a landward normal, and a cumulative **arc length**. That is exactly what `chain.js`
gets from its authored `shoreX(z)` curve and `colT()` — except derived from whatever
`WaterHeightFn` the app supplied, so it works for tree's wobbly island, forest's terrain and the
dome fixture without any of them declaring a shape.

Invalidated with the bed (level / extent / units, `water.c:496-498`) plus anything that moves
the bed itself.

**This is also the honest answer to the A1 question that stalled Part A.** The alongshore phase
refraction needs is an arc length along the shore; the shader had no line for it; this is the
line. Whether to spend it on wave phase is a later call — see Deferred.

## P6 — foam in the film's own frame

With P5's polyline, foam gets a `(band, column)` material coordinate and rides the swash instead
of sitting in world space. Per the reference's hard-won note, wave foam and film foam **erode
their own pattern in their own frame and only the resulting masks blend** — blending the
coordinates smears the pattern into streaks across the handover.

This also removes the per-cascade-texel tiling that forced every beach gate arm onto Gerstner.

## Status after P6 — and three arms that must go green first

Shipped: **P0** `3fae9ad`, **P1–P4** `66d0a22`, **P5** `81c2758`, **P6** `5b036a3`, spec `d789fcb`.
**All 24 goldens 0 px.** But the full suite came back with **three failures**, and they are the
work of P1/P2/P6:

```
water-shoal     roughness over the bed 0.0410 -> 0.0541 = 1.32x   (want <=0.88)
beach-shoreline brightest radial sample at the waterline: 9 of 18  (want >=13)
beach-surf-zone bed moves 0.582 of the shore band                  (want >=0.60)
```

**`water-shoal` is a real regression and gets fixed, not re-calibrated.** It asserts that
shoaling makes the surface SMOOTHER — the arm's own comment records foam "masking the shoaling it
sits on" as the failure it was written to catch, and roughness has now gone the wrong way by
1.32x. The suspect is P2: `OCEAN_BREAK_MIN_DEPTH_M` 0.5 and `OCEAN_BREAK_ON` 0.90 are letting
breaking foam cover the shoal window rather than a band inside it. Tighten the criterion until
the arm passes on its own terms; if it cannot be tightened without losing the Gerstner surf zone
that P2 exists for, that is a finding and the two get separated by depth.

**The two beach arms need diagnosing, not assuming.** I wrote both in 11.44 against continuous
foam, and `beach-shoreline` takes an ARGMAX along each radial ray — a statistic that necessarily
degrades when the ring becomes clumps, which is the change P1 was for. But 9 of 18 is half the
coastline, which is more than clumping alone should cost. Establish which it is before touching
either number: render the ring, count whether foam still REACHES the waterline at the failing
azimuths. If it does, the arm is measuring continuity and should be restated to measure reach
(the property it names); if it does not, P1's bar is still too high and moves.

`beach-surf-zone` at 0.582 against 0.60 is marginal and probably falls out of whichever of those
two is true.

## P7 — the swash film

A Lagrangian chain per shoreline column, in C against P5's polyline:

- Per segment, conserved rest volume; depth is `vol / length`.
- `eta = terrain + vol/length + shock viscosity`, `a = -g * d(eta)/dx`.
- **Pressure form, never a linear spring** — a preloaded linear chain has degenerate equilibria
  and settles into piled-up states. Recorded because it is the failure mode, from `chain.js:6`.
- Friction ramping landward; CFL caps on acceleration and velocity; alongshore smoothing,
  wrapping or clamping on `Water.shore_closed`.
- The seaward node is driven by **`shoreRunup().edge`**, not by the wave field. That keeps one
  forcing model for both wave paths and needs no CPU access to the spectral cascades, which
  exist only on the GPU — the reference reads its drive from a CPU noise texture and we have no
  equivalent.

### It rides a UBO, so Wall 1 is not touched

The ledger's own second escape: *"the 'no LUT' constraint this ledger imposes is a constraint on
**texture** lookups, and a table small enough to live in uniform/const space is not one"*, with
clustered forward named as the precedent that *"consumes **zero** units"*.

`pbr_frag` does not need the solver's 64 nodes — it needs the **tip per column at nine past
times**. At 64 columns that is 576 floats, **2.3 KB**. `ClusterBlock` is already
`uvec4 clusters[768]` = 12 KB and runs every frame; `pbr_frag` carries four uniform blocks where
GL 3.3 guarantees twelve.

So: **no sampler, no `#define` alias, no D0.** Unit 6's opaque slot is already spent on the cloud
shadow (11.41) and could not have been shared anyway — both read in the same pass. D0 was the
wrong thing to reach for; it is Effort M, frees exactly one unit, and carries a 0-px octahedral
bar the roadmap says should stop the item if unmet.

**`water_vert` reads the same block**, so the sea's lens and the sand's wetness are driven by the
same tips and agree by construction — which removes the one gap this phase was going to keep.

### The CPU twin, and the instrument that keeps it honest

The chain runs on the CPU, so `shoreRunup` needs a C twin — about 40 lines of the same
arithmetic. That is exactly the duplication CLAUDE.md flags on `procedural/water_waves.c`:
*"nothing compares the two, which is the known gap."*

**So this one ships with the comparison the existing one lacks:** `--shore-probe` prints the CPU
run-up at a few (position, time) samples, mirroring `--water-probe`, and a gate arm reads it
against what the shader draws. If the twin drifts, an arm says so.

`shoreRunup().edge` stays the **fallback**: no bed, no polyline, or the film off, and the closed
form still runs, so every existing frame is reachable and the phase is revertible by a flag.
`--no-water-film` is that flag.

---

---

## P6 and P7 as built, and the three things that had to be learned first

**P6 took three attempts and only the third is a design.** The first two both tried to give
foam the shore's own COORDINATE frame, and both failed on geometry rather than on taste:

- An **alongshore arc length** has a CUT where the traced chain closes. As a varying it is
  interpolated, so any grid cell straddling the cut sweeps the entire coast and the noise runs
  hundreds of cycles inside one cell -- a zigzag band of hairlines, stair-stepped at the bed
  texture's own texels.
- **Rotating world position into the shore basis** is worse. On a round island the position
  vector is radial, so its tangential component is identically zero, the pattern collapses to a
  function of radius, and the sea fills with concentric bullseyes. This is the same degeneracy
  the A1 entry below already records for the cusp axis.

What works is a **displacement, not a coordinate**: offset the lookup by how far the tongue has
run, along the bed's downhill. World position plus a smooth offset can neither collapse nor
tear, and it addresses the actual complaint -- a pattern held still in the world while the band
that reveals it moves is a stencil, and reads as one.

**P7's sampler problem had the wrong answer for most of a session.** Unit 6's opaque slot is
already spent on the cloud shadow and could not be shared regardless, and D0 buys exactly one
unit behind a bar the roadmap says should stop it. The ledger's own second escape fits: a table
small enough for uniform space is not a texture lookup. The tips are 2.3 KB against a cluster
block already carrying 12, so **`ShoreFilmBlock` costs zero from Wall 1** and both the sea and
the sand read it.

**Two defects the film's probe caught that reasoning had not.** The solver's dynamics were
written in METRES in a world of 22 units to one, so a 6 m/s CFL cap became 0.27 world-units/s
and the chain could not move a centimetre a frame -- the same mis-scaling 11.44 found across
the water's other physical lengths, arriving through the one quantity that is an acceleration.
And the first reading was taken 0.2 s after the seed, where a push at the handover needs about
three seconds to cross the beach at `sqrt(gh)`; the flat answer was the rest state, correctly
reported. Settled, the film reads **tip 8.40 to 12.29 units, mean 10.30**.

**The CPU twin ships with the instrument the existing one lacks.** `--shore-probe` prints it and
`shore-twin` reads it, checking what BOTH copies compute: the analytic ceiling, that the edge
moves, and that one primary period on it has moved somewhere else -- which only incommensurate
trains do. `procedural/water_waves.c` has been the first such duplicate since 11.32 with
nothing comparing it to the shader; repeating that silently was not worth 40 lines.

## The sampler wall that was not one

**P1 shipped procedural noise because `water_frag` was at 16/16, and that was the wrong conclusion
from a true premise.** The program really was full — 5 of its own, 9 from `ocean.glsl`, plus
`shadowMaps` and `cloudShadowTex`. What was not examined is *what it was full of*: six of those
declarations were the transformed cascades, `cascade0_0` through `cascade2_1`, all RGBA16F at 128²
and identical in format, size, filtering and wrap. Two more were the previous-frame pair.

A cap counted in DECLARATIONS is a cap on how many distinct **shapes** of data a program reads. Six
identical fields were never six shapes. As one `sampler2DArray` of six layers they are one
declaration holding the same six images, and with the previous pair as a second array the program
goes **16/16 → 10/16**, leaving units 2-6 and 9-10 free.

**What the wrong conclusion cost.** The foam pattern went to procedural ALU, which has no mip
chain — so the far field has no filtering and is protected only by foam fading out before it
aliases. That is the one row of the comparison against the reference that could not be matched, and
it was paid for a constraint that could have been removed first.

**The change is small where it matters.** The ping-pong is untouched except in where its output
lands: `glFramebufferTextureLayer` for `glFramebufferTexture2D`. The layer packing is
`cascade * 2 + target`, written once in `oceanCascadeLayer` where six sampler names previously
spelled it at every call site.

**Two costs, both real:**

- An array carries ONE mip policy for every layer, so the short band is mipped where it was not. It
  is read at LOD 0 regardless, so this is a third of a level of memory and no changed read.
- The FFT impulse test's scratch has to stay **fp32**. Built from the same fp16 helper as the sea's
  own fields it reported `mode_err` 4.8e-4 against 1.9e-7 for identical arithmetic — the storage's
  error wearing the transform's name, and it would still have passed the arm's 0.001 bound while
  silently blunting a precision test.

Verified at 24 goldens 0 px, the water group green, and the impulse back to `dc 0.0 / mode 1.9e-7`.
The roadmap's ledger section carries this as a fourth escape, and its Wall 1 now records that a
second, unnamed wall existed in a second program for four specs because "the sampler wall" was
written as a singular noun.

## Deferred, with reasons

- **A1, refraction's phase.** Snell conserves `k*sin(theta)`, and the code already computes that
  magnitude (`OCEAN_SURF_OBLIQUE` is `sin(theta)`). Only the *direction* is wrong, and fixing it
  needs an alongshore arc length. Worse, a per-point `kv` breaks the analytic derivative:
  `oceanSurfTrains` takes the phase gradient as `om*dTau - kv`, which assumes `kv` is constant in
  p, and the derivative feeds the normal. And `perp(shoreward)` degenerates — on a round island
  `p` is radial, so `dot(p, perp(s)) == 0` and the whole shoreline pulses in unison. Fundamentally
  a curved shore has no single-valued alongshore phase at constant wavenumber
  (`curl(t_hat) = div(n_hat) =` the shoreline's curvature), and where rays cross the phase is
  genuinely multivalued. **Revisit only after P5**, which supplies the arc length.
- **A2, refractive focusing.** Needs the bed's *second* derivative. `ocean.glsl` already records
  what happened when a per-point quantity was taken off tree's wobbly bed gradient: it *"printed
  every crest as a comb of humps at the wobble's period."* Curvature is noisier still, and the
  dome fixture is a surface of revolution where focusing is azimuthally uniform and therefore
  invisible. If it is built, bake it (the bed texture's `.a` is free) rather than sampling it.
- **A3, the Iribarren breaker type.** Clean and cheap, but it is calibrated to reproduce the
  present look at the present sea state, so it moves nothing until the sea state does. Worth
  doing; not worth doing before any of the above.

---

## Verification

```bash
./build.sh
python3 scripts/gates.py --only water      # after each phase
python3 scripts/gates.py --only beach
python3 scripts/gates.py                   # ONCE, at the end
python3 scripts/goldens.py
```

The full suite costs ~7 minutes — targeted groups between phases, whole suite once before the
merge.

- **Measure the noise floor at your own framing first.** Established this session:
  `water_fixture` and `beach_fixture` are both **0 px x2** on one build, so they are usable as
  exact instruments. `apps/tree` is not (31,034 px on the orbit path).
- Pin exposure (`--no-auto-exposure -E 1.0`) for every A/B. An unpinned comparison changes 99.77%
  of pixels and tells you nothing.
- **P1 and P2 must leave both water goldens 0 px** (Gerstner, no bed, no crest foam). **P4 must
  leave all 24 at 0 px** — nothing in the corpus opts in. *Both held: 24/24 through P6.*
- **The three red arms above are the gate on P7 landing at all.** Green them first; a solver
  built on top of a red tree cannot be bisected against anything.
- **P7 must leave all 24 goldens 0 px too.** Nothing in the corpus has a bed AND opts into
  wetness, and the film is gated on a traced shoreline that only the dome fixture and tree have.
- A/B crops at the surf framing before committing any look change:
  `--fov 31.5 --cam-eye -372.771,11.348,41.170 --cam-target -372.281,11.254,40.303`, and at
  `--sun-elevation 8` where tree is legible at all.
- `--profiler` before and after P4 (the taps are real ALU on the swash band) and P7 (a CPU
  per-column solver).
- `/thermo-nuclear-code-quality-review` and `/simplify` in **background** agents, all finishing
  before anything is applied.

## Critical files

- `cetra/shaders/include/shore.glsl` — `shoreRunup()`, landed in P0 (`3fae9ad`)
- `cetra/shaders/include/foam_pattern.glsl` — **new**, procedural pattern (no sampler left)
- `cetra/shaders/water_frag.glsl` — the shore band, at 16/16 samplers
- `cetra/shaders/water_foam_frag.glsl` — the existing history the rise term reuses
- `cetra/shaders/pbr_frag.glsl` — the wet-sand model; needs `uniform float time;`
- `cetra/src/water.c:488` — the bed bake, where the shoreline polyline gets extracted
- `cetra/src/render.c:145-270`, `:348`, `:407-422` — material uniforms, `time`, program switch
- `apps/tree/src/ground.c:147` (+ call sites `:232, :281, :367`), `ground.h:143`
- `assets/beach_fixture.cscn` / `gen_beach_fixture.py` — the fixture that must be able to see it
- `scripts/gates.py` — the `beach` group (added this session, `528197c`)

**P7 adds:**

- `cetra/src/shore_chain.c/h` — **new**, the Lagrangian solver over `Water.shore_pts`
- `cetra/src/shore_runup.c/h` — **new**, the CPU twin of `shoreRunup`, and the only place it is
  duplicated
- `cetra/shaders/include/shore_chain.glsl` — **new**, the std140 tip block, shaped like
  `include/lights_ubo.glsl` and read by both `pbr_frag` and `water_vert`
- `cetra/src/light_cluster.c` — the UBO create/upload/bind idiom to copy, not to modify
- `cetra/shaders/include/shore.glsl` — `shoreSwash` takes the tips when they exist
- `cetra/shaders/include/ocean.glsl` — `oceanSurf` fits its lens to the simulated tip
- `apps/render/src/render.c`, `apps/tree/src/tree.c` — `--no-water-film`, `--shore-probe`
