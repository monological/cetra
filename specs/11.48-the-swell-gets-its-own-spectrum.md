# 11.48 — The swell gets its own spectrum

## Context

`apps/tree` renders a large opaque white blob over its shallow shelf that moves with the waves.
Six commits of 11.47 did not touch it, because it is not crest foam.

Traced this session, at one camera, by elimination:

| render | result | conclusion |
|---|---|---|
| `--water-foam-debug 1` | flat green, 2 specks | not crest foam |
| `--no-water-surf` | blob gone, sea glassy | surf-gated → `Breaking` |
| `--gerstner-waves` | **clean parallel surf lines** | `Breaking` itself is correct |
| `--wind-speed 3 --fetch 4000` | blob unchanged | not amplitude — something ignores the wind |
| `--swell 0` | **blob gone, proper surf lines** | it is the swell train |

Frames kept at `_debug/src{1..6}_*.png`.

> **The fourth row's conclusion was half wrong, and P6 below is the correction.** What that
> Gerstner frame showed was `Breaking` at an amplitude too small to fill a contour, not
> `Breaking` behaving. Its SELECTION — when a wave breaks — is correct and is the textbook
> depth-limited criterion. Where the whitewater then goes, and what it is made of, were both
> wrong, and no amount of calming the sea would have fixed them. Read this table as "the swell
> is *a* cause", never as "the swell is *the* cause".

**Root cause, `water.c:271-273`:** the swell train is seeded from a hardcoded 8.4 m/s wind over
310 km of fetch. It reads neither `sea->wind_speed` nor `sea->fetch`. `WATER_DEFAULT_SWELL` is
0.38 so `sw_weight` is *exactly 1.0* at the default — full strength, always.

`Breaking` (`ocean.glsl:769`) tests `disp.y / (0.39 · depth)`, the depth-limited criterion. Over
tree's 1–3 m shelf that swell genuinely *is* breaking, everywhere. **`Breaking` is reporting the
truth about an absurd sea**, which is why no amount of work on the foam path could fix it.

Two things this corrects:

- **Commit `839c6fd` overclaimed.** "The swell answers the sea state" made the `swell` *knob*
  scale the train, but at the default `sw_weight` is 1.0, so nothing changed for any app that
  does not author `swell`. tree does not.
- **`swell` is overloaded.** `water.c:241` already uses it as the directional-spread sharpener —
  which is what it means in both reference implementations. `839c6fd` added `:273` using it
  *again* as the swell train's amplitude. Turning it down now also broadens the wind sea's
  spread, two unrelated effects on one knob.

**What the references do** (`research/water/`):

- **gpuocean** — not applicable. Scrolling band-limited noise, no spectral model.
- **inkwell** — has our exact bug, because we ported it verbatim: `8.4` / `310_000` / gamma `2.6`
  / `+0.82` rad / spread `+9.0` / gain `0.72`, and our library defaults are its defaults. It does
  not have the *problem*, because its wind sea is hardcoded at 11.5 m/s too. One fixed sea, one
  fixed swell, calibrated against each other. **We broke that when 11.42 made wind and fetch
  authorable — we kept the proportion's numerator and made its denominator a knob.**
- **poseidon** — solves it. The swell is a **complete second parameter set**
  (`params.js:22-44`), and `fillSet` (`spectrum.js:98-107`) derives `alpha` and `peakOmega` from
  **each set's own** `windSpeed` and `fetch`. The two spectra are summed (`:133-140`). Its swell
  runs at wind 2.0 m/s over 300 km — a *low* wind over a *long* fetch, which is what swell is.

Swell as a fraction of wind sea: poseidon **19%**, inkwell **55%**, cetra-in-tree **300%**.

**Outcome:** the swell is a wave train like any other, authored per scene, and a calm sea is
calm.

## Design

One type, twice. Today's swell block (`water.c:252-291`) is 40 lines of near-duplicate
JONSWAP + spread math; it collapses into **one density function called twice**, and the
asymmetries between the two trains become *data* rather than divergent code.

```c
// One wave train's spectrum. The wind sea and the swell are the same kind of thing, so
// they are one type twice rather than one type and a hardcoded imitation of it.
typedef struct WaterWaveTrain {
    float wind_speed;       // m/s at the standard 10 m reference height
    float fetch;            // metres of open water the wind has blown across
    float direction;        // radians, RELATIVE to Water.wind_dir
    float scale;            // spectral density weight; 1 is the spectrum as the fetch law gives it
    float peak_enhancement; // JONSWAP gamma
    float focus;            // 0..1 directional narrowness (Horvath's parameter)
    float spread_gain;      // outer factor on the spread power
    float spread_blend;     // 0 = a broad cos^2 lobe, 1 = the focused cos^2s one (see Results)
} WaterWaveTrain;

typedef struct WaterSeaState {
    float sea_depth;        // metres; drives the TMA correction. A property of the WATER,
                            // not of a train, so it is not in WaterWaveTrain.
    WaterWaveTrain wind_sea;
    WaterWaveTrain swell;
} WaterSeaState;
```

Defaults chosen so the sea stays today's:

| | `wind_speed` | `fetch` | `direction` | `scale` | `peak_enhancement` | `focus` | `spread_gain` | `spread_blend` |
|---|---|---|---|---|---|---|---|---|
| `wind_sea` | 11.5 | 120 000 | 0.0 | 1.0 | 3.3 | 0.38 | 0.58 | 0.68 |
| `swell` | 8.4 | 310 000 | 0.82 | 1.0 | 2.6 | 0.833 | 0.72 | 1.0 |

**Everything is bit-exact except the swell's directional spread.** The old swell used a flat
`+9.0` where the wind sea uses `16·tanh(min(ω/ωp,20))·focus²`; unifying the form is what makes
this a parameter set rather than two code paths. Fit `focus` numerically across the band before
pinning the constant.

**The overload dies:** `focus` is the spread knob (the wind sea's, and the swell's own);
`swell.scale` is the swell's amplitude. `swell.scale = 0` disables the train, where today
`swell = 0` does — so that is a change of meaning, deliberately.

**`secondary_scale` stays per-cascade** (`WATER_CASCADE_CFG`, 0.22/0.08/0). It is a *band*
weighting — which cascades carry the second train at all — not a property of the train, and it is
what keeps the swell out of the 12 m cascade. `swell.scale` multiplies on top of it.

## Phases

### P1 — one density function, two trains

`water.h` / `water.c`. Extract the JONSWAP + directional-spread + short-fade body of
`_water_build_spectrum` (`water.c:174-348`) into `_water_train_density(train, omega, theta_abs,
tma, wind_angle)`; call it twice and sum. `_water_peak_omega` and `_water_jonswap` already take
the pieces they need — pass a `WaterWaveTrain*` rather than a `WaterSeaState*`.

Three traps, all confirmed:

- **`_water_seed_is_current` (`water.c:1032-1039`) is a hand-written field-by-field float
  comparison**, deliberately not a `memcmp` (its comment rejects a dirty flag). Every new field
  must be added or editing it silently never re-seeds. Consider a `_water_train_is_current`
  helper so the two trains cannot drift.
- **`_water_surf_state` (`water.c:1324`) calls `_water_peak_omega(&water->sea)`** → becomes
  `&water->sea.wind_sea`. Behaviour unchanged. *Recorded, not done:* physically the surf should
  follow the longer train (the swell), and at these defaults the swell's period is 8.3 s against
  the wind sea's 6.7 s — but that moves `waterSurfOmega` and every shore frame with it.
- Log lines at `water.c:1092-1096` and `:1120-1122` print wind/fetch; say which train.

### P2 — the scene-file surface

Nested, symmetric:

```json
"water": {
  "waves": "fft", "seaDepth": 54.0,
  "windSea": { "windSpeed": 11.5, "fetch": 120000, "direction": 0.0, "scale": 1.0,
               "peakEnhancement": 3.3, "focus": 0.38, "spreadGain": 0.58, "spreadBlend": 0.68 },
  "swell":   { "windSpeed": 8.4,  "fetch": 310000, "direction": 0.82, "scale": 1.0,
               "peakEnhancement": 2.6, "focus": 0.833, "spreadGain": 0.72, "spreadBlend": 1.0 }
}
```

Top-level `windSpeed` / `fetch` / `peakEnhancement` / `swell` are **removed**. `parse_water`'s
closed `known[]` block then warns on any scene still using them, which is the loud migration this
wants — and there are exactly two such scenes, both ours.

*Alternative rejected:* flat prefixed keys (`swellWindSpeed`, `swellSpreadGain`, …). It avoids
the gate work below, but gives eight awkward keys and leaves the wind sea's fields unprefixed, so
they read as belonging to the water rather than to a train — which is the exact confusion that
produced this bug.

Files: `cscene.h` (`CSceneWater`), `cscene.c` (`parse_water` + `known[]`),
`cscene_apply.c:354-361`, both fixtures **and both generators**
(`assets/gen_{water,beach}_fixture.py` — the committed `.cscn` is byte-compared against a fresh
regeneration).

**`_cscn_water_key_sets` (`gates.py:4072-4084`) must learn to nest.** Its regex is
`\(water,\s*"([A-Za-z]+)"`, so nested keys are invisible and `water-fixture-roundtrip` fails until
taught. Extend it to the sub-objects rather than loosening it — the point of that gate is that
`parse_water`, its `known[]` list and the fixture cannot drift apart.

Note `_water_cscn_variant` shallow-merges the gate's override dicts, so
`WATER_SEASTATE_CALM` (`gates.py:3470`) must author a whole `windSea` object, not one key.

### P3 — GUI

`gui.c:610-623`. Two `igTreeNode` sections, eight sliders each, plus the shared depth. Direct
pointer binds as today.

### P4 — apps/tree authors its own swell

tree's wind sea is 6 m/s over 15 km. The swell it inherits is ~1.1 m effective. Author a
proportionate one (start near `wind_speed 3.0`, `fetch 40000`) and **tune by eye against the
A/B**, not to a target number. Its `--swell` flag becomes the train's `scale`; `--wind-speed` /
`--fetch` address `wind_sea`.

The `--no-water-surf` flag added to `tree.c` while tracing this is the instrument that found it;
it landed on master ahead of this branch rather than inside it.

### P5 — docs

`AGENTS.md:654-661` and `:922` state "21 keys", "authors all 21", "the five sea-state keys". All
go false. `CLAUDE.md` is a symlink to the same file.

### P6 — and a breaker gets its own shape (added after approval)

**This phase was explicitly out of scope above, and the risk that put it there fired.** With
P1–P5 in, tree still showed the artifact: much smaller, but present, and described from the
window as **"a spotlight moving across the ocean"**. That is the diagnosis, not a complaint —
a soft-edged patch translating over the shelf is exactly what a level set of a smooth height
field over a static bed looks like.

The swell was a real defect and fixing it took ~1.1 m of unasked-for swell down to 0.16 m. What
remained is two defects in how `Breaking` is DRAWN, neither of them in when it is selected.

**1. It composited as a swash, not a whitecap.** `water_frag.glsl:766` read

```glsl
shoreFoam = max(shoreFoam, WATER_SHORE_FOAM * Breaking);
```

so a breaking crest went on at the shore band's `WATER_SHORE_FOAM_MAX` of **0.88**, through a
pattern coordinate that rides the swash sheet up the sand. Spec 11.47 split the crest and shore
composites apart on precisely the argument this violates — *"a whitecap and a swash are different
materials, a slick of bubbles millimetres thick with sea showing through, against a decimetre of
aerated water"* — and then left this line feeding the wrong one. It becomes

```glsl
foam = max(foam, Breaking);
```

which routes it into the crest band: the **0.30** ceiling, and the world-plus-drift erosion
coordinate that breaks a whitecap up as it travels rather than stencilling it against the beach.
Unscaled, unlike the shore band's `WATER_SHORE_FOAM`, because the crest band is already a 0..1
coverage fraction.

**2. It was selected by height alone, so it was a filled level set.** In `ocean.glsl` only
`disp.y` varies with time — `bed.column` and `enough` are static — so thresholding the ratio
fills every point of a broad smooth hump and the region translates with the crest. The height
test says a wave MAY break; it cannot say where the water whitens. That is the steep shoreward
face, and the unshoaled derivative already carries it:

```glsl
float bedFall = length(bed.dColumn);
vec2 seaward = bedFall > 1.0e-5 ? bed.dColumn / bedFall : vec2(0.0);
float front = max(dot(vec2(dispDx.y, dispDz.y), seaward), 0.0);
float charSlope = max(0.5 * waterSurfHeight * waterSurfOmega * waterSurfOmega / 9.81, 1.0e-4);
float face = smoothstep(OCEAN_BREAK_FACE_ON * charSlope,
                        OCEAN_BREAK_FACE_FULL * charSlope, front);
s.breaking = smoothstep(OCEAN_BREAK_ON, OCEAN_BREAK_FULL, disp.y / breakLimit) * enough * face;
```

`bed.dColumn` is the gradient of the water COLUMN, so it points seaward, and a point on the
shoreward face climbs toward the crest in that direction — hence the positive dot. The back face
gives a negative one and stays dark, which is what turns a filled contour into a crest line with
a wake. Taken from the UNSHOALED derivative to match the `disp.y` the ratio uses: both describe
the incident wave, which is what the criterion is about.

**The threshold is RELATIVE, and that is the load-bearing choice.** `charSlope` is the surf's own
deep-water steepness `a·k`, with `a = Hs/2` and `k = ω²/g` — unitless, because Hs is in metres
and k is per metre, so it needs no unit conversion in a world whose unit is not a metre. An
absolute slope constant would break the same wave on a millpond and never on a gale, which is the
identical mistake `AGENTS.md` already records against the inherited 0.115 far-field roughness
literal. `OCEAN_BREAK_FACE_ON = 1.0`, `OCEAN_BREAK_FACE_FULL = 2.5`: shoaling steepens a front by
roughly two to three before it overturns, so a face carrying more than its deep-water share is
the one actually breaking.

**Not done, and it is the third defect the risk named: there is still no accumulator.**
`Breaking` remains an instantaneous function of the current displacement, so whitewater does not
outlive the crest that made it. It cannot simply ride the existing foam history — that is a
cascade-space target and therefore TILES the world, where `Breaking` is anchored to the bed. A
world-space accumulator is real work and is the honest remainder of this.

## Files

- `cetra/src/water.h` / `water.c` — the type, the density function, defaults, the re-seed compare
- `cetra/src/cscene.h` / `cscene.c` — `CSceneWater`, `parse_water`, `known[]`
- `apps/render/src/cscene_apply.c` — the `.cscn` → `Water` bridge
- `cetra/src/gui.c` — the sliders
- `apps/tree/src/tree.c` — its sea state and flags
- `assets/{water,beach}_fixture.cscn` + `assets/gen_{water,beach}_fixture.py`
- `scripts/gates.py` — `_cscn_water_key_sets`, `WATER_SEASTATE_*`, `_water_cscn_variant`
- `AGENTS.md`
- **P6 only:** `cetra/shaders/include/ocean.glsl` (the face term and its two constants) and
  `cetra/shaders/water_frag.glsl` (the composite slot). `water_foam_frag.glsl` is untouched.

## Verification

```bash
./build.sh
python3 scripts/goldens.py                 # 24/24 at 0 px -- both water goldens are Gerstner
python3 scripts/gates.py --only water
python3 scripts/gates.py --only beach      # P6: the only group that reaches Breaking at all
```

- **Goldens must stay 0 px.** Both water goldens are Gerstner and never reach the spectral
  seeding, so this is a containment check: if either moves, the change leaked out of the FFT path.
- **`water-fixture-roundtrip` is the P2 acceptance test** — it fails until `parse_water`, its
  `known[]`, the fixture and the generator all agree, at BOTH nesting levels.
- **`beach_fixture` is `"waves": "fft"`** and authors a sea state, so it moves. Its `beach-*` arms
  override to Gerstner by flag and should not — verify that, do not assume it.
- **The A/B is the acceptance test for P4 and P6**, at the camera these were taken with:
  `--sun-elevation 35 -W 2000 -H 1280 --fov 31.5 --cam-eye -88.746,31.602,-50.250
  --cam-target -89.642,31.465,-50.673`. Before is `_debug/src1_baseline.png`; the target is
  `src6_noswell.png` **with visible swell still in it** — a beach with no swell is a lake.
- **P6 needs a FORCING framing as well as the default one**, because the default sea rarely
  reaches the criterion: `--wind-speed 12 --fetch 100000` at the same camera puts a 3 m sea on
  the shelf and makes the selection unmissable. A fix judged only at the default would not have
  been judged at all.
- `apps/tree` is **not** pixel-deterministic (~21 k px floor from `glfwGetTime()` in the drag
  controller). Judge it by eye; `water_fixture` is the 0 px instrument.

## Branch

**`11.48-the-swell-gets-its-own-spectrum`**, off `master`.

*The plan as approved branched off the `11.47-whitecaps-that-break` tip, because 11.45 → 11.47
were then three unmerged branches that had to land in order. They were merged before this work
started, so master already carries that tip and the ordering constraint is gone.*

P6 is on this branch rather than on an 11.49 of its own. It was scoped out, it turned out to be
required to finish the thing this spec set out to fix, and splitting it after the fact would have
put the branch's own defect fix somewhere the branch does not point.

## Risks and falsifiers

- **The `focus` fit is worse than the flat `+9.0` across the band.** The two forms agree at the
  peak by construction and diverge in the tails. *Falsified by:* `water-fft-var`'s height ratio
  moving outside its band, or the swell visibly changing character in the fixture. *Response:*
  keep `spread_flat` as a ninth per-train field (0.0 / 9.0) and be bit-exact.
- **Removing the top-level keys breaks a scene nobody remembered.** *Falsified by:* grep says two
  fixtures, both ours. The `known[]` warning makes any survivor loud rather than silent.
- **tree still blobs after P4.** Then the swell was not the whole story and `Breaking`'s shape is
  implicated after all. *Response:* the three defects listed for `Breaking` earlier (no
  accumulator, swash opacity, area-not-face) become 11.49 — but do not pre-emptively fix them,
  the Gerstner and `--swell 0` frames both say `Breaking` is correct.
  → **THIS FIRED.** Two of the three are P6 above; the accumulator is not done. The "do not
  pre-emptively fix them" instruction was right for the order of work and wrong about the
  evidence: "the Gerstner frame says `Breaking` is correct" was never a sound inference, because
  that frame only showed it at an amplitude too small to fill a contour.
- **`water-seastate` gets weaker, not stronger.** Would mean the wind sea is not what moves those
  20 000 px. Worth knowing either way.

## Results

**24 goldens 0 px. 177 gate arms pass; `beach-shoreline` fails**, at 7 of 18 azimuths after
P1–P5 — byte-identical to the line it failed on master before this branch — and at 6 of 18 after
P6. Pre-existing, nudged one sample by P6, not caused here.

Four predictions were wrong, and each is worth more than the phase it belonged to.

- **The struct needed EIGHT fields, not seven.** The wind sea blends a focused lobe with a broad
  `cos²` one at 0.68/0.32 and the old swell used the focused lobe alone — an asymmetry the plan
  did not notice. poseidon settles it: one `directionSpectrum` for both sets, with the blend as
  per-set data (`spreadBlend`, `spectrum.js:50`). So `spread_blend` is a field, and the wind sea
  stays bit-exact because `1.0f - 0.68f == 0.32f` exactly in float32.
- **`water-fft-var` did NOT move and needed no re-baseline** (0.96/0.91/0.80 height,
  0.99/0.97/0.93 slope, identical to 2 dp). The reason is structural and should have been
  predicted: the directional factor is normalised over the circle, so reshaping the lobe moves
  the total variance only through discrete sampling. That also makes it a **weak falsifier for
  the `focus` fit** — it cannot see the change it was nominated to catch. What can is the
  per-cascade probe.
- **`water-seastate` did not get stronger** — 340,557 → 341,222 px, which is noise. The plan
  assumed the swell was diluting the arm; it still is, because the arm lowers `windSea.windSpeed`
  and leaves `swell.scale` at 1. Making the swell authorable does not change an arm that does not
  author it. Neither the predicted improvement nor the falsifier's weakening: it simply does not
  bear on this.
- **`spread_flat` was not needed.** The `focus` fit is 0.833, energy-weighted least squares over
  the 880 modes that actually carry the swell rather than the peak-only 0.859 the plan proposed
  (weighted RMS error in the spread power 0.879 against 1.053). 72% of the swell's energy sits
  within a quarter of its peak. The residual is a lobe half-width of 28-38 degrees where it was
  a flat 32.

**The containment check is the per-cascade probe, not a golden.** Both water goldens are Gerstner
and never reach the seeding, so they prove only that nothing leaked. Cascade 2 has
`secondary_scale` 0, carries no swell in either version, and `--water-fft-probe` reads it
**identical to every printed digit** across the change — which is what pins the wind sea. Cascades
0 and 1 move by 0.016% and 0.005% in predicted height, the swell's reshaped lobe and nothing else.

`apps/tree`: Hs 0.66 m with the authored swell, 0.64 m without it, against a wind sea the plan
estimated at 0.375 m from the fetch law alone — the measured figure is 4σ over the seeded
cascades and is the one to quote.

### P6's results, and the coverage hole it exposed

- 24 goldens 0 px. Both water goldens are Gerstner **with no bed**, so `Breaking` is structurally
  zero in them and the whole block never runs.
- **All 33 water arms pass at numbers IDENTICAL to before P6 — and that is a finding, not a
  reassurance.** `water_fixture` is Gerstner at amplitude 0.06, which never reaches the
  depth-limited criterion, so the water group has **no coverage of breaking whitewater at all**.
  It measured containment. Two shader edits to the surf path moved it by exactly zero.
- `beach_fixture` does reach it, and barely moved: `beach-shoal` 0.7962 → 0.7987,
  `beach-surf-zone` 0.770 → 0.762.
- `beach-shoreline` 7/18 → 6/18. It looks for a continuous bright ring at the waterline, and surf
  that is crest lines rather than sheets is structurally harder for it to see — so it may be the
  arm that needs rethinking rather than the renderer. It was already red either way.

**The follow-up this earns: no gate asserts anything about breaking whitewater directly.** The
one instrument that reaches it does so incidentally, through arms written for shoaling and for
the shore band. A `water-breaking` arm — that the selection fires over a bed and not over open
water, and that it is a band rather than a filled region — is what would have caught both of P6's
defects at the time they shipped.

## Recorded after approval

**`beach_fixture` over-foams badly on its own FFT path** — `./out/bin/render -m
assets/beach_fixture.gltf --water-bed dome` painted a blown-out white ring over the whole shoal.
The fixture authors 10.5 m/s over 90 km → Hs ≈ 1.61 m, and inherited ~0.91 m of swell on top, so
the swell was only ~13% of the combined sea here rather than tree's 300%, and **P4's swell fix
did not clear that frame** — as predicted at the time. **P6 does**, which is the second piece of
evidence that the remaining defect was never the sea state.
