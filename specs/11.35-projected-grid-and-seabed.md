# Spec 11.35 — a projected grid, and an island that has a seabed

> **Renumbered from 11.34.** This work was authored as 11.34 on a branch cut before that
> number was taken; `specs/11.34-single-sample-target.md` (E9, one sample means one sample)
> landed on `master` while this branch was in flight and closed at commit `c986f56`. Both
> specs touch `cetra/src/water.c` and `scripts/gates.py`, so leaving the collision would have
> made every "spec 11.34" comment in the water path ambiguous about which spec it meant.
> Commit messages on this branch dated before the renumber still say 11.34 and mean this file.

## Context

The water surface reaches 5.1° short of the horizon, and `apps/tree:412` claims it does
not. That comment — "Past the dome's rim, so the sea reaches the horizon rather than
ending in a visible edge" — was asserted, never measured. Measured: eye height over the
water is ~97 units, the clipmap is camera-centred with half-extent 1100, so its far edge
sits at `atan(97/1100)` = 5.1° below eye level while the horizon is at 0°. That is ~290 px
of an 1800 px frame showing skybox where there should be sea.

**The reach cap is structural, and it is the same decision 11.33 was proudest of.** Rings
tile exactly only because every level snaps to the *coarsest* cell, and that cell is
`extent·2/gridRes`. So reach and near-field stability are welded together: pushing extent
toward the horizon (~318,000 units at this eye height) makes the finest cell 311 units and
the 90-unit swell disappears. Adding ring levels does not help — the snap granularity
tracks the outermost extent either way.

**The clipmap was not a wrong fix; it was a fix aimed by a procedural goal.** 11.32 shipped
a uniform world-space grid, which put vertices where the world is rather than where the
pixels are — a real, measured defect. The clipmap fixed it and the fix worked. But 11.33 was
framed as "close every gap 11.32 left open", so it built the deferral that had a *name*.
Nobody wrote down how far the water must reach, so nothing tested the choice against it. A
projected grid solves the same density problem *and* the reach problem, and the clipmap's
central mechanism — rings as windows into a mip pyramid of streamed data — buys nothing for
a surface that is procedural and evaluable at any point.

**Intended outcome:** the water reaches the horizon, `apps/tree` reads as a domed island
with a real seabed under the whole sea, and no claim about reach survives unmeasured.

## Non-goals

- **Shore breakers, swash, a nearshore SWE solver.** Still a different simulation.
- **Jolt buoyancy.** `water_surface_at` is the seam; wiring bodies to it is not in scope.
- **Spherical-earth curvature.** The horizon will be an infinite-plane horizon. Bending the
  far field to a planet radius is a look decision worth its own change.
- **Keeping the clipmap.** One surface path. A projected grid handles bounded water too, by
  clamping the ray/plane solve; a `--water-mesh` mode would be two vertex paths, two sets of
  arms, and the kind of "temporary" branch that becomes permanent.

## What makes this cheap: the varyings are placement-agnostic

`water_vert.glsl` outputs seven varyings and **all of them derive from one world-space XZ
point** via `oceanShoal(p)` / `oceanEvaluateAt(p, …)` / `oceanPreviousWorldAt(p, …)`:
`WorldPos`, `ViewPos`, `Normal`, `CurrClip`, `PrevClip`, `Jacobian`, `Shoal`. Nothing
downstream of `water_vert.glsl:64` knows how `p` was chosen. `Jacobian` and `Shoal` have no
clipmap dependency at all.

So this replaces **how `p` is produced** and nothing else. The whole of `water_frag.glsl`,
`ocean.glsl`'s evaluation, the cascades, absorption, caustics, the medium and the motion
vectors are untouched. The `aGrid` attribute (`location 0`, `vec2`, stride 8) is kept and
reinterpreted from "grid coordinate in [-0.5, 0.5]²" to "lattice coordinate in NDC", so the
VAO/VBO layout does not change either.

## Phases

### Phase 1 — The projected grid

Replace `water_ensure_grid`'s two index sets with one screen-space lattice, and
`clipmapPosition` with a ray/plane solve.

**Geometry — keep the vertex buffer, delete the annulus.** `water_ensure_grid`'s VBO is
already a unit-square parameter lattice with a `vec2`-only attribute (`water.c:510-516`,
`location 0`, stride 8). That is exactly a projected grid's input. So the VBO and VAO survive
untouched and only the *interpretation* changes, plus the second index set goes: one index
set, one `glDrawElements`, no instancing, no rings, no snap. Resolution becomes a
*pixels-per-cell* quality knob rather than a world size.

**Placement needs ZERO new uniforms, using this codebase's own ray idiom.** The house style
is not to upload an inverse — it is the focal-reciprocal reconstruction, and
`cloud_march_frag.glsl:49-51` is the purest existing form:

```glsl
vec2 ndc = TexCoords * 2.0 - 1.0;
vec3 viewDir = normalize(vec3(ndc * invFocal, -1.0));
vec3 rd = normalize(mat3(invView) * viewDir);
```

`invFocal` is `1.0 / vec2(projection[0][0], projection[1][1])`, computed *in the shader* from
the forward projection — which `water_vert.glsl` already has. And view→world rotation is
`mat3(transpose(view))`, the trick `water_frag.glsl:286` already uses for its reflection
lookup. So the ray is built from uniforms the pass uploads today, and `waterCamPos` (already
uploaded) is the origin. Intersect the water plane, take world XZ as `p`, and the existing
evaluation runs unchanged.

Note there is **no ray-plane intersection helper** anywhere — CPU or GPU. `intersect.h` has
`ray_aabb_intersection` and `ray_point_at_distance` only. This is ten lines written fresh,
placed in `ocean.glsl` beside the surface it feeds. `create_fullscreen_quad_vao` is a
4-vertex triangle strip and is *not* reusable as a lattice.

**The far plane does not cap this, and that is the part worth getting right.** Camera far
planes here are 2000–3000 (`tree.c:920`, `forest.c:748`), so a vertex at true horizon
distance would be clipped. `skybox_vert.glsl` already solves exactly this with
`gl_Position = pos.xyww` — pin z to w and the vertex lands at the far plane in NDC while
keeping its screen position. So: rays whose plane intersection is beyond a cap (or which are
near-parallel to the plane) get their clip-space z pinned, which puts them precisely on the
vanishing line. The horizon row *is* the horizon, at any far plane.

**Dead code this deletes:** the T-junction stitch (`water_vert.glsl:72-112`) exists purely to
reconcile ring boundaries, and it currently costs two extra full `oceanEvaluateAt` +
`oceanPreviousWorldAt` evaluations on one vertex row per patch. The five ring uniforms go with
it, along with `water.h:26-40`'s snap rationale block and `ring_index_count`. The stitch's
*idea* is not lost — Phase 4 hands it to D4, where it fixes a gap the roadmap has open.

**Degenerate cases the clipmap got for free and this must handle explicitly:**
- Camera exactly on the plane, or within a wave height of it — rays are parallel, every
  intersection diverges. Clamp against a minimum ray-plane angle.
- Camera below the plane looking up — the surface is overhead; the intersection sign flips.
  `water_frag` already handles submerged shading (`cameraSubmerged`), so only placement needs
  the sign case.
- Displacement pushes edge vertices off-screen, leaving gaps at the frame border. Over-scan
  the lattice slightly beyond NDC ±1.

**Arms.** `water-draws` re-anchors from `2 / 5 / 131072` to the projected grid's own
integers — 1 draw, 1 instance, a fixed lattice triangle count. Note the constraint the
counter block records at `water.c:1042`: `meshes_seen` must equal `instances + culled` or the
profiler's own sum check fires. `water-crack` survives untouched — its comment is explicit
that it detects a *hole* (a pixel that equals the no-water frame), not a T-junction, so it
covers horizon and near-plane tearing just as well.

**New arm, `water-horizon` — the one that would have caught this bug.** On a fixture framed
so the horizon is in shot, assert there is no band of sky-coloured pixels between the
waterline and the horizon: walk a column from the shore upward and require the last water
pixel to be within N pixels of the horizon row. The current build fails this by ~290 px.

### Phase 2 — Wave-field mips, and the far field as a BRDF

A projected grid's far vertices each cover kilometres, and **the cascade fields have no
mipmaps** — `_water_make_field` sets `GL_LINEAR` with no `glGenerateMipmap` (`water.c:598`).
Sampling LOD 0 there is pure aliasing.

- Generate mips on the two displacing cascades after the spectral transform completes, and
  switch `MIN_FILTER` to `GL_LINEAR_MIPMAP_LINEAR`. `GL_REPEAT` and mips coexist fine, and the
  fields are FBO render targets so the regeneration has to be sequenced after the ping-pong
  ends (`water.c:781-796`). **Costs no sampler unit** — water declares 13 of a hard 16 with
  units 11, 14 and 15 free, so mipping textures it already binds is free where a separate
  coarse field would not be.
- Select LOD per vertex from the lattice cell's world footprint against each band's
  `length_scale` (240 / 64 / 12 m, `water.c:43-45`), with `textureLod` in the vertex stage.
  **No precedent exists** — every `textureLod` in the tree is in a fragment shader, and every
  current vertex-stage fetch is plain `texture()`, i.e. implicitly LOD 0.

**Mipped displacement converges to the mean, which is the still plane** — so the far field
flattens *by construction* rather than by a special case, and the energy it loses is exactly
what the existing geometry-to-BRDF handover picks up: `WATER_SHORT_NEAR/FAR` already fades the
short cascade's slope into roughness past 118 m. Phase 2 extends that mechanism to the long and
medium bands rather than inventing a second one.

**This is the riskiest phase, and there is a recorded failure mode to avoid.**
`water_frag.glsl:153-157` documents that driving roughness from the *faded* slope instead of
from the energy the fade removed made distant water go toward the calm value in both channels
— so the horizon got smoother as its waves went sub-pixel, which is backwards. Read that
comment before writing the selector: the mip must hand energy *over*, not dim both ends.
Verify by measuring surface roughness in a far-field box, not by looking at it.

### Phase 3 — Precision at horizon distances

`oceanCascadeUv` does `fract(p / cascadeLength + 0.5)`, which bounds the *lookup* but not `p`.
At 300k units, `p / 240` is ~1250 and `fract` of that has already spent the mantissa the near
field needs.

Sample the cascades relative to a camera-snapped origin: subtract a world offset snapped to
the cascade period before dividing, so the argument stays small and the field stays anchored
(`fract` is periodic, so a period-aligned offset is a no-op on the result).

**Genuinely new ground.** There is no camera-relative rendering, no world-origin rebasing and
no fp32-precision discussion anywhere in the tree — `docs/aaa-rendering-roadmap.md` has zero
matches for "precision", and everything is absolute world space in fp32. At 10 km a 240 m tile
leaves very few mantissa bits inside the tile, so this is a real requirement of reaching the
horizon rather than a tidy-up. No pattern to copy.

### Phase 4 — `extent` stops meaning two things

`extent` currently conflates two meanings. The geometry bound dies with the rings; **the bed
field domain survives and becomes the only meaning** (`water.c:417-423`, `ocean.glsl:83`,
`water_waves.c:80`, `water.h:215`). `ocean.glsl:84` already clamps out-of-domain reads to the
nearest edge texel, so a surface reaching past `extent` is already safe — it reads open water.

Corrections this forces, each currently stating the old meaning:
- `cscene.h:174` — "half-size of the drawn surface" → the shoaling bed's domain.
- `render.c:151` — `--water-extent` help text, same.
- `apps/forest/src/forest.c:699` — the comment promises the water stops where the terrain
  does. It will not; re-argue or drop the promise.
- `apps/tree/src/tree.c:415` and `:1120-1124` — the extent comment becomes obsolete, and the
  wavelength comment reasons from the clipmap's cell size, which no longer exists.
- `AGENTS.md:602-605` — "The grid is a camera-snapped clipmap … `--water-extent` is a size
  control again". Both halves become false.
- `specs/11.33`'s Phase 1 and its `# As built` section, and `docs/aaa-rendering-roadmap.md`
  D3 (`:848-872`) and the priority table (`:1200`). **Supersede narrowly and honestly: the
  clipmap works, and nothing recorded about it was false.** The argument is that a projected
  grid is better — density exact rather than in 2× steps, no T-junctions, one draw, and reach
  decoupled from near-field detail. Do not cite 11.32's `:450-457` deferral as support; 11.33
  already established it was wrong.

**What the deleted clipmap leaves behind for D4 (terrain).** Worth two sentences in the
roadmap rather than dead code in the tree, because terrain is the item a clipmap was actually
invented for — `docs/aaa-rendering-roadmap.md:886` lists Asirvatham & Hoppe as D4's *first*
reference. The part water never used is the part terrain needs: rings as windows into a mip
pyramid of streamed height data. So:

- Record in the D4 entry that the full clipmap implementation — the snap arithmetic and the
  T-junction stitch — is at commit `8d04658`, as D4's reference implementation. Git is the
  archive; keeping the code live for a hypothetical consumer is the "temporary branch that
  becomes permanent" the review round flagged.
- **Point D4's own open T-junction gap at the stitch, because it is a better fix than the one
  the roadmap currently proposes.** `:976-980` and `:1265-1266` admit that `lod.c` passes
  options 0, so only `meshopt_SimplifyLockBorder` would lock a border, and *"two neighbours at
  different LOD levels can T-junction … None has been observed at any framing tried, which is
  large tiles and near-straight borders rather than a guarantee."* `LockBorder` fixes it by
  never simplifying the border, which keeps full border density forever. The water stitch lets
  both sides simplify and makes the finer side *agree* — evaluate the two even neighbours and
  average, which is by definition the straight line the coarser edge draws. That requires a
  surface evaluable at arbitrary points, which `terrain_height_at` is by construction
  (`procedural/terrain.h:42-46`, "a pure function of (params, x, z)").
- Do **not** pre-shape any of this into a shared primitive now. Terrain today is a different
  architecture — fixed tiles with meshoptimizer LOD chains (`procedural/terrain.c`) rather than
  rings over a resampled grid — and D4 cites both Hoppe and Strugar's CDLOD, so it has not
  chosen. Extracting a "shared" clipmap primitive would be guessing at that choice.

### Phase 5 — `apps/tree`: a dome that reads as a dome

Today: `GROUND_RADIUS 900`, `GROUND_HEIGHT 20` — a slope of 0.044, **2.5° at its steepest**,
never below `y = -20`. No rim, no flank, no mass below the water.

- Give the dome a profile that reads as an island from the app's framing (camera 600 back,
  eye 95, canopy 250). Raise the rise-to-radius ratio and/or shrink the radius.
- **Re-derive `TREE_WATER_LEVEL`.** It is a closed form off the paraboloid —
  `-GROUND_HEIGHT · t²` puts the shoreline at `t · GROUND_RADIUS` (`tree.c:399-411`). Any
  profile change invalidates it. Keep the *derivation* rather than hard-coding a new number.
- **Check the bed bake against the steeper ramp.** `WATER_BED_RES 256` over `2 × extent` is
  8.6 units/texel here. The shoal window spans 2.56 units of depth, which on today's 0.044
  slope is ~58 world units — about 7 texels. A steeper dome shrinks that run, and at some
  slope the bake under-resolves the shoreline. Measure it; raise `WATER_BED_RES` or the bake
  domain if it bites.
- Factor `generate_island_mesh` (currently `static` in `tree.c:267-378`) and
  `ground_height_at` into the `ground.c` that `ground.h` has always implied. `apps/CMakeLists.txt`
  globs `src/*.c`, so no CMake edit — only a re-configure.
- **Fix two stale things while in here.** `ground.h:9-12` says "Flat (0) beyond the rim"; the
  code returns `-GROUND_HEIGHT`. And the falling-leaves particle module collides against a
  **hardcoded plane at y = 0** (`tree.c:859-863`) instead of `ground_height_at` — already
  wrong, and a deeper dome makes it visibly wrong.

### Phase 6 — `apps/tree`: a seabed under the whole sea

There is no geometry past `r = 900` while water is drawn well beyond it. Today the depth
buffer there holds the far plane, so `path` saturates at `WATER_MAX_PATH = 64`
(`water_frag.glsl:85`) and the open water reads as uniformly maximally-absorbed body colour.
**A real seabed changes the appearance of the entire open-water region** — which is why this
phase belongs with the grid rather than after it.

- Build the seabed with `mesh_builder.h` (`mb_init` / `mb_vertex` / `mb_tri` / `mb_transfer`
  — it writes tangent `w = +1` and computes the AABB), following `create_island`'s node
  pattern (`tree.c:433-449`): file-static node, uploaded once, kept out of the tree-rebuild
  path for the reason `tree.c:456-461` records.
- **One height authority.** `ground_height_at` must remain the single function the island
  mesh, the grass roots, the seabed mesh and `tree_bed_height` all read, or the shoreline
  drifts from the ground it meets. Extend it with the seabed profile rather than giving the
  seabed its own.
- Steal `terrain_normal_at`'s approach (`procedural/terrain.h:48-50`) — central differences on
  the height function — instead of hand-deriving the normal the way `generate_island_mesh`
  does at `tree.c:314-316`.
- Reuse `island_albedo_tex` / `island_normal_tex` with a darker `material->albedo`; a third
  procedural generator only if that reads badly. Opaque, single-sided, no wind, no subsurface.
- Caustics currently have nothing to land on out there and will start showing.
- Check the shadow ortho fit: `ortho_size = GROUND_RADIUS`, `far_plane = 6000` with a 14-line
  derivation at `tree.c:985-997`. A seabed past 900 pushes on it.

## Files

| file | change |
|---|---|
| `specs/11.35-projected-grid-and-seabed.md` | **new** — this plan, committed alone first |
| `cetra/src/water.c/h` | delete `water_ensure_grid`'s ring machinery, `WATER_RING_LEVELS`, `ring_index_count`; one lattice; new submission integers; cascade mip generation |
| `cetra/shaders/water_vert.glsl` | `clipmapPosition` → ray/plane solve; delete the T-junction stitch and the ring uniforms; far-plane pinning |
| `cetra/shaders/include/ocean.glsl` | the placement helper, LOD selection, camera-relative cascade sampling |
| `cetra/src/cscene.h`, `apps/render/src/render.c` | `extent`'s documented meaning |
| `apps/tree/src/ground.h` + **new** `ground.c` | dome profile, `ground_height_at` with the seabed, the island and seabed mesh builders |
| `apps/tree/src/tree.c` | re-derived water level, seabed node, leaf-collision plane fix |
| `apps/forest/src/forest.c` | the extent promise |
| `scripts/gates.py` | re-anchor `water-draws`; new `water-horizon` |
| `docs/aaa-rendering-roadmap.md`, `AGENTS.md` | D3 as-built correction; the clipmap paragraph; **D4** gains the clipmap's provenance (`8d04658`) and the stitch as a candidate fix for its own T-junction gap |

## Verification

```bash
./build.sh
# the horizon claim, measured rather than asserted
python3 scripts/gates.py --only water
python3 scripts/goldens.py      # water_fixture WILL move; re-bake and record the number
# cross-build, exposure pinned, on a scene with no water at all
./out/bin/render -m my_models/raiden/source/raiden_textured_rigged.glb \
    -t my_models/raiden/textures -e my_models/studio_small_03_8k.hdr \
    -a my_models/animations/strut_walk.fbx -s my_models/animations/T-Pose.fbx \
    -x -f 120 --no-springs --no-auto-exposure -E 1.0 -S after.ppm
```

Per phase: the full gate suite and all goldens. **Measure the noise floor first** — and note
`apps/tree` has no gate arm and no golden at all, and its own run-to-run floor is ~20k px
(`glfwGetTime` drag damping, `tree.c:1209`), so tree is verified by eye plus the deterministic
fixture arms, not by pixels.

**The horizon claim gets a number, not a comment.** The bug being fixed is a comment that
asserted reach without measuring it; the fix is not done until `water-horizon` prints the
remaining gap in pixels.

## Workflow

Per CLAUDE.md: spec to `specs/11.35-projected-grid-and-seabed.md`, **committed alone** before
any code. `master` now carries the water subsystem, so branch off `master`. Signed commits,
house message form, `review:` and `gates:` as their own commits, and an `# As built` section
per phase recording where this plan turns out to be wrong — including, specifically, whether
the far-plane pinning behaves at grazing angles and whether mipped displacement really does
converge to the flat far field the plan claims.

# As built — Phase 1

## What the plan got right

The claim that this would be cheap held. `water_vert.glsl`'s seven varyings really do all
derive from one world XZ point, so replacing *how `p` is produced* touched nothing else:
`water_frag.glsl` is byte-unchanged, and so are the cascades, absorption, caustics, the
medium and the motion vectors.

Placement needed **zero new uniforms**, exactly as predicted — `invFocal` off the forward
projection, `mat3(transpose(view))` for view→world, `waterCamPos` as the origin. The
`pos.xyww` idea from `skybox_vert` generalised into a clamp rather than a pin
(`min(clip.z, clip.w * (1 - 1e-5))`), which is better: it engages only past the far plane,
needs no branch, and leaves the surface one depth code inside the skybox's own `LEQUAL`
write so water still wins there.

**Reach invariance: 71 px → 0 px.** Both extents now report the water's top edge at row 135
of 600, which is where the fixture camera's horizon is.

## What the plan did not know

**The winding flips, and getting it wrong is invisible-by-looking.** A lattice in NDC maps
straight to window coordinates; a world-XZ grid maps through the view, and for a camera
looking down −Z its screen orientation is mirrored. So the clipmap's index order came out
*back*-facing on the projected grid — and `water_frag.glsl:190` flips its normal on
`!gl_FrontFacing`, so every water fragment in the frame was shaded upside down. It did not
look broken. It looked like a sea: the reflection simply came out of the environment's
*lower* hemisphere, which put the sky's ground colour into the far field as a khaki speckle
band and turned the near field into hard-edged navy blotches. Three plausible wrong
diagnoses came first (far-field aliasing, insufficient lattice density, a coverage gap), and
what killed all of them was one measurement: **the same submerged framing with near-flat
Gerstner waves, where the clipmap and the projected grid agree to ≤ 1/255 across the whole
interface.** That isolates placement from reconstruction, and it is the test to reach for
first next time.

**The projector aims at the still plane, and that is a real coverage gap the plan missed.**
A wave can lift geometry into view from XZ the lattice never sampled: the nearest surface a
ray can reach is `clearance / tan(topEdgeAngle)`, and anything nearer than that is simply
not in the mesh however deep its troughs go. The clipmap covered it for free by being a
world grid of fixed extent. It bites only when the eye is within roughly a wave height of
the still plane — irrelevant for `apps/tree` (95 units up) and for the fixture's own
above-water framing (Gerstner, 0.11 units of envelope against 1.35 of clearance), and
decisive for the submerged FFT arm at 1.65 units under a metre-scale sea.

The fix is Johanson's: aim the projector at the boundary of the *displaceable slab* nearest
the eye rather than at the still plane, which costs **no lattice rows** — the row→distance
mapping just shifts nearer by the displacement, and the horizon end does not move because a
grazing ray misses both planes. It was **not done here** because it needs a vertical
displacement bound, and the spectral path has none to publish: it would mean accumulating
the seeded realisation's variance and tracing the inverse FFT's normalisation, and an
over-estimate silently costs far-field density while an under-estimate silently costs
coverage. Filed rather than guessed. `water-submerged` moved to a framing where its claim is
observable instead (see `WATER_SUBMERGED_FLAGS`).

## Numbers

| measurement | before | after |
|---|---|---|
| `water-horizon` reach invariance | 71 px | **0 px** |
| water top edge, authored extent | row 207/600 | row 135/600 |
| draws / instances / triangles | 2 / 5 / 131072 | 1 / 1 / 131072 |
| vertex invocations per frame | 81,920 (+ stitch) | 66,049 |
| near-field row means vs the clipmap | — | ≤ ~1/255 |
| `water_fixture` golden | — | 194,967 px, PAE 0.427 (re-baked) |
| every other golden | — | 0 px |

`WATER_GRID_RES` went 128 → 256 so the triangle count is unchanged at 131,072; the vertex
count still falls, because the clipmap paid for 5 patches of the same grid.

## Still open after this phase

- The far field genuinely does alias now that it reaches the horizon — Phase 2, and the
  Gerstner path needs its own handover, since the plan's mip-the-cascades answer only
  covers the spectral one.
- The still-plane projector's coverage gap, above.
- `extent`'s meaning: `water.h`'s field comment was corrected here because it directly
  contradicted this change; the rest of the list is still Phase 4.

# As built — Phase 2

## What the plan got right

Mips on the displacing cascades cost **no sampler unit**, exactly as predicted — they are
textures water already binds. `GL_REPEAT` and mips coexist. The sequencing constraint the
plan named was real: these textures are the ping-pong's own render targets, so generation
has to follow the 14th stage or it filters a half-transformed field.

**Mipped displacement does converge to the still plane**, which the plan asked to have
verified rather than assumed. At the top level a 128² field is one texel — its mean — and for
a wave spectrum that is zero displacement. So the far field flattens with no special case.

The recorded failure mode was avoided: roughness is driven by what the filtering REMOVED,
never by what survives. Measured as the far field's sensitivity to the authored roughness
falling 8.2× — out there it is the handover that sets roughness.

## What the plan did not know

**The plan had no answer for the Gerstner path.** "Mip the cascades" only covers the spectral
one, and `apps/tree` — the app that motivated the whole spec — is Gerstner. Its analogue had
to be written fresh: drop an octave whose wavelength is under a couple of cells, faded over
wavelength/footprint from five cells to two, applied to the derivatives as well as the height.
The fraction of slope variance dropped is then exact rather than estimated, because a Gerstner
octave's slope amplitude is `a·k` in closed form. That turned out **better** than the spectral
side, where the split between what a mip level keeps and removes is not directly available and
`filtered` is the normalised level instead.

**Every cascade read had to become explicit-LOD, including the fragment stage's.** Not for the
obvious reason. `oceanCascadeUv` wraps with `fract`, so an implicit screen derivative reads a
whole period across each tile seam — the moment those textures carry mips, that is a blurred
line through every seam in the frame. This is a trap that would have shipped looking like a
lighting artifact.

**The footprint cannot come from the distance.** A projected grid's cells are anisotropic by
orders of magnitude near the horizon (radial span against lateral), so the footprint is taken
from the placement itself: two extra ray solves per vertex, no extra fetches.

**A bisect lever was added that the plan did not ask for**, and it earned itself immediately:
`--no-water-lod` reports a zero footprint, which is exactly full detail on every band, and it
reaches the pre-phase-2 frame at **0 px**. That single number proves the whole phase changes
nothing except through the footprint — no mip-generation side effect, no `textureLod`
transcription error. It is also what `water-farfield` reads against, so the arm compares two
renders of one build instead of trusting a threshold.

## Numbers

| measurement | unfiltered | filtered |
|---|---|---|
| far-box spatial spread (aliasing) | 0.0308 | **0.0075** (4.1×) |
| near-box max channel delta | — | **0** (byte-identical) |
| far-field sensitivity to authored roughness | 0.01131 | **0.00132** (8.2×) |
| `--no-water-lod` against the phase-1 frame | — | **0 px** |
| `water_fixture` golden | — | 88,128 px, far band only (re-baked) |
| every other golden | — | 0 px |

## A look observation, with its cause

What survives in the MID field is octave 0 alone, and at grazing incidence a single-direction
Gerstner train reads as regular swell lines. It is not lattice terracing — the stripes
converge with perspective, so they are wave crests — and it is the corduroy `ocean.glsl`
already warns about. It reads as an improvement over the speckle it replaced (static and
coherent rather than shimmering), and the real fix is a directional spectrum, which is what
the FFT path already has. Not changed here: which model `apps/tree` asks for is a look
decision for its own phase.

## Still open after this phase

- The still-plane projector's coverage gap (phase 1's note) is untouched.
- `filtered` on the spectral path is the normalised mip level, not a measured variance split.
  Exact would need the second moment of the slope field, which the cascades do not carry.

# As built — Phase 3

## The plan's premise was wrong, and the measurement is the useful part

The plan said this was "a real requirement of reaching the horizon rather than a tidy-up".
It is not. The lattice's far rows do sit at a million units, but **phase 2 filters them to the
still plane**, so the precision available out there buys nothing — a flat surface is a flat
surface at any mantissa. Reaching the horizon costs nothing in precision for a camera near the
world origin, which is every camera in this tree.

What actually degrades is a camera far from the world **origin**. That is measurable, because
the spectral path is exactly translation-invariant across the cascade LCM of 960 units (240,
64 and 12 all divide it, so all three `fract` arguments are unchanged). Same surface, same
view direction, geometry out of frame on both sides:

| | differing px of 480,000 | RMSE | PAE |
|---|---|---|---|
| 960 → 307,200 units out, before | 230,054 | 0.01499 | 0.337 |
| 960 → 307,200 units out, after | **227,458** | 0.01460 | 0.302 |

So the division `p / cascadeLength` was the **small** term — 3%. The snap removes it exactly
and for free, and all 23 goldens stay at 0 px, which is the periodicity claim checked rather
than asserted.

## Where the rest of the error is

`p` itself, not the division. It is built as `waterCamPos.xz + rd.xz * t`, and the camera term
dominates: at a quarter-million units fp32 quantises it to 0.03 units, which is 6% of the
medium band's 0.5 m texel — enough to move a slope, and a grazing reflection is very sensitive
to a slope.

**Attributed rather than guessed.** A flat surface (`amplitude: 0`) at the same two offsets
moves 20,483 px at PAE 4/255, against the waved surface's PAE 77/255. So it is the field
lookup, not the view transform's cancellation.

The fix is to carry the lattice position camera-relative, so error tracks distance from the
EYE rather than from the world origin. Not done here, and the reason is not effort: absolute
fp32 world coordinates are what every mesh in such a world is built from, so water alone would
be precise standing next to terrain that is not. That is camera-relative rendering, and it
deserves its own spec. Recorded with the full recipe so it does not have to be re-derived.

## Two traps worth carrying forward

**A translation test has to move the geometry too, or it measures the wrong thing.** The first
version compared the origin frame against a shifted one and read 65% of the frame differing —
which was the fixture's ramp leaving the view and its mip'd refraction resolve going with it,
not precision at all. The usable comparison is two SHIFTED frames against each other.

**The scene format has no model transform**, so the geometry cannot be translated from a
`.cscn`. That is what forces the two-shifted-frames shape, and it is why the arm for this
does not exist: a photometric gate here would be pinning a known defect's magnitude.

## Still open after this phase

- Camera-relative positions, above.
- The still-plane projector's coverage gap, from phase 1.

# As built — Phase 5

## What the plan got right

`ground.c` was overdue and the split was clean: `apps/CMakeLists.txt` globs `src/*.c`, so no
build edit. Keeping the water level's **derivation** rather than a new number was the right
call and paid immediately — the shape changed twice during this phase and the sea followed
both times with no separate edit.

Both stale things it named were real. `ground.h` claimed "Flat (0) beyond the rim" while the
code returned `-GROUND_HEIGHT`; the falling leaves collided against a literal `y = 0`.

## What the plan did not know: the three numbers are not independent

The plan said "raise the rise-to-radius ratio and/or shrink the radius", which reads as two
free knobs. They are not. Writing `rise = H·t²` and the above-water width as `2·t·R`, the
beach slope is `2·rise / (t·R)` — determined by those two alone, whatever `H` and `R`
separately are. Two constraints then pin everything:

- **Fit the frame.** The frame spans ~620 units across at the island's distance. The first
  attempt (t = 0.5 over R = 900, so a 900-unit island) put the shore out of frame on both
  sides and read as a **flat coastal plateau** — worse than the sandbar it replaced. Measured
  by rendering it, which is the only reason it did not ship.
- **Be shoal-able.** The water's shoal window is 2.56 units of *depth*, so on slope `s` it is
  `2.56/s` wide, against `2·extent/WATER_BED_RES` per bed texel. Under ~3 texels the shore
  band collapses to one linear segment.

Those give slope ≈ 0.31 and rise ≈ 48 units. Final: **R 620, H 190, shore at t 0.5** — a
310-unit shoreline (where 11.32 had it), a 17° beach, and a crown 47.5 units clear of the
water, which is 14% of frame height.

**The bed extent had to come down, 1100 → 400**, and that is the second half of the shoal
constraint rather than a separate tweak: it is the *shore band's* domain, not the island's.
3.1 units per texel puts ~3 texels on an 8.4-unit ramp. The plan expected to raise
`WATER_BED_RES` instead; tightening the domain was cheaper and strictly more accurate where it
matters, and left a library constant alone.

## The leaf collider, which turned out better than "use ground_height_at"

There is no heightfield collider — the primitives are sphere, box, plane, capsule, cylinder.
But a paraboloid with `H ≪ R` **is** a sphere: radius `R²/(2H)` tangent at the crown tracks
this dome to **0.01 units under the canopy and 2 units at the rim**. So the litter collides
against an analytic collider the particle system already had, derived from the same two
constants, and it follows the profile automatically.

# As built — Phase 6

## The plan's premise is wrong, and this is the third time in this spec

It said "A real seabed changes the appearance of the entire open-water region — which is why
this phase belongs with the grid." Measured against the same frame without it:

| box | red | green | blue |
|---|---|---|---|
| band over the seabed | **+0.0** | **+1.1** | **+0.9** |
| further out (grazing, Fresnel-dominated) | 0.0 | 0.0 | 0.0 |
| sky (control) | 0.0 | 0.0 | 0.0 |

Whole frame 38,646 px of 720,000 (2.7%), and part of that is auto-exposure — `apps/tree` does
not pin it, so a large dark mesh shifts the meter and the canopy's high-contrast edges show it
as speckle. The structural part is the band.

**The cause is structural, not a tuning miss.** `WATER_MAX_PATH` clamps the optical path at 64
units while the water past the rim is 233+ deep, so the bed sits beyond the absorption's reach
everywhere except a shore band the island's own flank already covered. The channels that moved
are exactly the ones with transmission left at path 64 — blue 0.278, green 0.107, red 0.0017 —
which is the arithmetic agreeing with the pixels rather than a coincidence. **No seabed at any
depth can do better from this framing** without changing the water's optical properties, which
is a look decision and not "add a seabed".

What it does buy: one height authority now has geometry everywhere it claims ground (the
water's shoaling field was already reading a bed out there that nothing drew), and a camera
that goes under has a floor rather than void.

## One thing the plan asked for that the numbers refused

The plan wanted the seabed's depth chosen; it should be **derived**. The ease `1 - (1-u)²`
leaves the rim with slope `2·DROP/span` and the dome arrives there with `2H/R`; equating them
gives `DROP = (H/R)·span`. Written as that expression, because a hand-picked drop puts a kink
at the rim silently — and the first version had one, with a comment claiming it did not.

## Also checked, and left alone

The shadow ortho fit (`ortho_size = GROUND_RADIUS`, `far_plane = 6000`) needed no change: its
derivation is `2·(radius + treeHeight/tan(elevation))`, dominated by the 250-unit tree, and the
radius went *down*. Caustics do not reach the open water either — `WATER_CAUSTIC_DEEP_OFF`
closes them at a path of 20.

# Closing — what this spec delivered, and what it withdrew

**Delivered.** The surface reaches the horizon, asserted as reach invariance at **0 px** where
the clipmap failed by 71. The far field is filtered rather than aliased (spatial spread 4.1×
lower) with the slope energy handed to roughness (8.2× less sensitivity to the authored value).
`apps/tree` is a domed island with a beach and a seabed. Two new gate arms, one new bisect
lever that reaches the pre-change frame at exactly 0 px, and 22 water arms green.

**Withdrawn or corrected.** Three of six phases had premises that did not survive measurement,
and in each case the measurement is the more useful artifact: phase 3's precision work is not
needed to reach the horizon and fixes 3% of the error it was aimed at; phase 6's seabed moves
1/255 in two channels rather than the whole open-water region; and phase 5's "two free knobs"
are one. Phase 1 also found a bug the plan could not have predicted — every water fragment
shaded with an inverted normal — which looked entirely plausible on screen.

**Filed rather than half-done.** The still-plane projector's coverage gap (a wave can lift
geometry into view from XZ the lattice never sampled), and camera-relative positions for worlds
far from the origin. Both have full recipes recorded above.
